import type { ReactNode } from "react";

export const metadata = { title: "Account ledger" };

export default function AccountLedgerLayout({ children }: { children: ReactNode }) {
  return children;
}
