import type { ReactNode } from "react";

export const metadata = { title: "Reconciliation run" };

export default function ReconciliationRunLayout({ children }: { children: ReactNode }) {
  return children;
}
