# UI and UX review against the research on intuitive design and cognitive load

Status: method, measurements and heuristics registered 2026-09-12 before any finding was recorded and before any change was made. Results are appended below the line "Results"; nothing above that line changes after it.

## Why this review exists

`docs/15-function-audit.md` established that every function in the prototype does what its label says and that the screens agree with their own contracts. It did not ask whether the interface is easy to read, easy to learn, or easy on a tired person at 4 pm. The owner asked for that next, in these words: review the UI and UX; compare to research regarding intuitive website design and design for reducing cognitive load; make the interface accessible, reasonably customizable, and modern with a clean, extremely low cognitive load; ensure the experience is consistent and very intuitive.

Each of those words is given a measurable meaning below, so that the review can fail. "Modern" and "clean" are the two that resist measurement; they are treated as the consequences of the others (few colour roles, one type scale, one spacing rhythm, no ornament that carries no information) rather than as targets of their own.

## What is being measured, and how

Every measurement is taken from the live prototype under headless Chromium at three widths — 1280 px (desk), 1024 px (operatory) and 420 px (phone) — in both themes, on the seeded data, before and after the changes. The same scripts run both times, so the report compares numbers rather than impressions.

| Instrument | What it measures | Where it lives |
|---|---|---|
| `scripts/a11y-check.mjs` (`npm run a11y`) | axe-core 4.13.0 (vendored under `scripts/vendor/axe-core/`, MPL-2.0) run against 17 surfaces × 2 themes × 3 widths with the WCAG 2.0/2.1/2.2 A and AA rule sets plus axe's best-practice rules; every distinct violation with the criterion it maps to and the first offending node. Exit 1 on any serious or critical violation. Part of `npm test`, after `proto-check` and before `reproduce`. | repository, permanent |
| `scripts/syntax-check.mjs`, the `P-*` gates in `reproduce.mjs` | Already in place from the owner's stability round: every file parses; boot time, route paint time, allocation cost and reset isolation stay within budget. A visual change that slows a paint fails here. | repository, permanent |
| `scripts/proto-check.mjs` | Already in place: 44 px targets with 8 px gaps at four widths, text contrast at 4.5:1 / 3:1 by size, no animation under reduced motion, no horizontal page scroll, focus ring on every control and no focus trap, a `data-testid` on every control, the five flows within their tap budgets. | repository, permanent |
| `measure.cjs` (review scratch) | Per screen: controls on the page and above the fold; buttons by identity (irreversible, reversible, quiet, held); top-bar control count and how many rows it wraps to; cards and the maximum and mean controls per card; chips and the size of the chip vocabulary; longest button label in words and every label over three words; heading count, `h1` word count, sub-line word count; prose words above the fold; longest prose run standing before a finish button; distinct text and background colours painted; font sizes in use; page height as a multiple of the viewport. | recorded in this document's tables |
| Baseline screenshots | 17 surfaces × 3 widths × 2 themes, full page, taken before any change, so a reviewer can see what a number describes. | `knowledge/reviews/ux-review/baseline/` |
| Heuristic audit | One agent per screen scores every registered heuristic with the measurement or the screenshot that supports the score; eight cross-cutting lenses (visual hierarchy, density, typography, colour and contrast, motion, keyboard and focus, consistency of components, first-screen comprehension) look across screens; findings are deduplicated into root causes and every root cause is put to an adversarial verifier whose default position is "refuted". | `knowledge/reviews/ux-review/` |

## How the research was gathered, and what that means for the citations

The environment's network policy blocks most of the web, including w3.org, nngroup.com, developer.mozilla.org and the NHS, GOV.UK and USWDS design-system sites. It allows GitHub and the npm registry. The research agents were therefore instructed to fetch primary sources from GitHub — the WCAG 2.2 normative text and understanding documents from `w3c/wcag`, the axe-core rule descriptions from `dequelabs/axe-core`, and the source settings and component documentation of `nhsuk/nhsuk-frontend`, `alphagov/govuk-frontend` and `uswds/uswds` — and to mark every heuristic in one of three ways:

- **fetched primary source** — the page was fetched in this session and the quoted text appears on it verbatim; a critic re-fetched every such quote and struck any it could not find;
- **fetched secondary source** — as above, but the page is a design system's or vendor's reading of the research rather than the research itself;
- **training knowledge, unverified** — the heuristic is one the field relies on (Hick's law, Miller's span, Nielsen's ten) but no primary source could be fetched; it is included because the owner would rather see it labelled than omitted, and it is never presented as verified.

A heuristic's label is printed beside it in the table below. Readers who need the primary text for an unverified row should treat it as a claim to check, not a fact established here.

## The words, made measurable

| Owner's word | Meaning adopted here | Where it is measured |
|---|---|---|
| Accessible | WCAG 2.2 Level AA with no serious or critical axe violation; AAA where it costs nothing (enhanced contrast on body text, 44 px targets already exceed 2.5.8's 24 px minimum) | `a11y-check`, `proto-check` targets/contrast/focus/motion |
| Reasonably customizable | A person can set text size, density, contrast, motion, colour scheme and a colour-vision aid; each honours the operating-system preference first, persists per user, and never drops a shared or operatory device below the 44 px floor; the settings surface itself has at most eight choices | settings probe in the audit; `proto-check` targets on every setting combination |
| Modern, clean | One type scale, one spacing rhythm, at most three button identities visible on a screen, no decorative element that carries no information, no ornament competing with data | `measure.cjs` identities, font sizes, colours |
| Extremely low cognitive load | The counts the heuristics bound: choices per group, controls per card, prose on the finish path, top-bar controls, chip vocabulary, sub-line words | `measure.cjs` |
| Consistent | One word per concept (already policed by `docs/15`), one identity per verb, one placement per recurring control, one colour role per meaning across every screen and both themes | consistency lens in the audit |
| Very intuitive | A first-day temp can name what each screen is for and what to do first from the screen alone; every state change is acknowledged; every irreversible action is distinct; recognition never recall | first-screen lens; `proto-check` flows |

## Status vocabulary for findings

| Status | Meaning |
|---|---|
| breach | The measurement is on the wrong side of the heuristic's threshold, or a reviewer scores the screenshot against a "judgement" heuristic and an adversarial verifier does not refute it |
| meets | The measurement is on the right side of the threshold |
| not measurable here | The heuristic needs a real user, a screen reader, or data the seed does not hold; recorded as a gap, never as a pass |

## Declared constraints

| Constraint | Effect |
|---|---|
| Reviewers are language-model agents driving headless Chromium and reading screenshots | They measure structure and read images; they do not experience time pressure, gloves, or a screen reader |
| Primary sources are limited to what GitHub hosts | Every row says whether it was verified; the unverified rows are not fewer, only labelled |
| The prototype is static and seeded | Only seeded states exist; a finding about a state the seed cannot produce is recorded, not fixed |
| Two rounds of automated review preceded this one | Some of what this review would have found is already fixed; the baseline it measures is the merged state after `docs/15` |

## What this review cannot prove

Real usability, time on task, learnability in one shift, screen-reader behaviour, satisfaction, or that any person finds the result "intuitive". It can prove that the interface is on the right side of every measurable threshold the research supports, that it was on the wrong side of some of them before, and that the changes did not regress anything the earlier harnesses guard.

---

## Registered heuristics

Registered 2026-09-12, after the research pass and before the audit ran. The audit scores the prototype against these 117 rows and no others. The full table, with each row's measurement procedure, sits in [`knowledge/reviews/ux-review/registered-heuristics.md`](../knowledge/reviews/ux-review/registered-heuristics.md); the machine-readable form the audit reads is [`registered-heuristics.json`](../knowledge/reviews/ux-review/registered-heuristics.json) beside it.

How the rows were made, and what the counts mean:

| Step | Result |
|---|---|
| Seven research agents, one body of research each, fetching primary sources from GitHub (see "How the research was gathered") | 170 heuristics, 152 of them marked verified by their authors |
| Three independent citation critics re-fetched every one of the 152 quotes, defaulting to "not found", matching verbatim after stripping tags, collapsing whitespace and ignoring case and quote marks, with at most one word of difference allowed | 152 checked, 0 struck |
| The reviewer re-fetched four rows chosen at random with a separate script | 4 of 4 found |
| One agent found duplicates across the seven bodies: rows a single probe would settle together | 33 groups, 52 rows dropped, the row with the fetched source and the more precise test kept in each; 23 overlapping pairs kept with the tension recorded |
| Registered | **117 heuristics**: 72 fetched primary source, 29 fetched secondary source, 16 training knowledge, unverified; 68 must, 48 should, 1 may; 0 with a threshold of "judgement" |

Three things the reader should know before the table. First, a "must" row is one whose source states a requirement (a WCAG Level A or AA criterion, a design system's setting); a "should" row is one whose source recommends or whose threshold the researcher set from the prototype's own baseline numbers, and the audit reports which. Second, the unverified rows are the Nielsen, Norman, Miller, Cowan, Hick, Fitts and dyslexia-guidance rows whose primary sources sit behind the network block; each is labelled so and none is presented as established. Third, several rows measure the same surface from different bodies of research and were deliberately kept apart because their tests differ (for example, text contrast at rest and text contrast on hover and focus); the duplicate finder's notes on those pairs are in the working set and the audit scores each row once.

Where the fetched sources disagreed on a number, the stricter number a fetched source supports was kept and the disagreement is written into the kept row's threshold or the duplicate finder's note; the three disagreements that matter most are the focus-ring width (WCAG 2.4.13 says at least 2 px, GOV.UK's and NHS's settings say 3 px, and 3 px is kept), the body text size (GOV.UK, NHS and USWDS set 16 px or larger at the root while this prototype is at 15 px, and 16 px is kept), and button height (USWDS about 40 px, GOV.UK about 36 px plus its shadow, NHS 44 px, and the NHS figure, which is also this prototype's own `--target`, is kept).

### WCAG 2.2 (Level AA, plus cheap AAA)

| Id | Level | Heuristic | Passes when | Evidence |
|---|---|---|---|---|
| WCAG-1.4.3 | must | Give every piece of text a contrast ratio of at least 4.5:1 against what it sits on (3:1 for text at or above 24px, or 18.66px bold), in both the light and the dark theme. | >= 4.5:1 for text under 24px (or under 18.66px bold); >= 3:1 for text at or above 24px or >= 18.66px and bold. 0 failing text nodes per theme. | primary ([source](https://raw.githubusercontent.com/w3c/wcag/main/guidelines/sc/20/contrast-minimum.html)) |
| WCAG-1.4.6 | should | Aim for 7:1 contrast on body and label text (4.5:1 for large text) so text stays legible under operatory lighting, glare and aging eyes. | >= 7:1 for normal text, >= 4.5:1 for large text; pass when >= 90% of visible text nodes meet it and primary body text (--ink) and form labels are at 100%. | primary ([source](https://raw.githubusercontent.com/w3c/wcag/main/guidelines/sc/20/contrast-enhanced.html)) |
| WCAG-1.4.11 | must | Make every visual cue that identifies a control or its state — input borders, button outlines, chip rails, selected and pressed states, the refusal left rail, table rules that carry meaning — contrast at least 3:1 with the colours next to it. | >= 3:1 for every cue that is the only means of identifying the control or its state, in both themes; 0 failing cues. Input borders must pass because an empty text box has no other boundary. | primary ([source](https://raw.githubusercontent.com/w3c/wcag/main/understanding/21/non-text-contrast.html)) |
| WCAG-1.4.11-focus | must | Make the keyboard focus ring contrast at least 3:1 with the background it is drawn over, in both themes and inside dialogs. | >= 3:1 between outline-color and every adjacent background it can be drawn over; 0 failing focus positions per theme. | primary ([source](https://raw.githubusercontent.com/w3c/wcag/main/understanding/21/non-text-contrast.html)) |
| WCAG-1.4.1 | must | Never let colour be the only thing that tells severity, state or reversibility apart; every severity chip, refusal rail and button identity must also carry a glyph, word or shape. | 100% of severity chips and refusal blocks carry a non-colour cue; 100% of .btn.irreversible show the bar glyph; severity luminance order is strictly monotonic in both themes (0 inversions). | primary ([source](https://raw.githubusercontent.com/w3c/wcag/main/guidelines/sc/20/use-of-color.html)) |
| WCAG-1.4.4 | must | Let text be enlarged to 200% with nothing clipped, overlapped or lost, on every device profile. | At 200%: 0 clipped text elements, 0 overlapping controls, 0 controls lost; body horizontal scroll width <= viewport width. | primary ([source](https://raw.githubusercontent.com/w3c/wcag/main/guidelines/sc/20/resize-text.html)) |
| WCAG-1.4.12 | must | Survive a user text-spacing override (line-height 1.5, paragraph spacing 2em, letter-spacing 0.12em, word-spacing 0.16em) without clipping or losing anything. | 0 clipped or overlapping elements after the override; every control still readable and clickable. | primary ([source](https://raw.githubusercontent.com/w3c/wcag/main/guidelines/sc/21/text-spacing.html)) |
| WCAG-2.4.7 | must | Show a visible focus indicator on every keyboard-focusable element; no control may remove the outline without an equally visible replacement. | 100% of Tab stops show an outline >= 2px (prototype standard is 3px); 0 rules removing outline on Tab-reachable elements. | primary ([source](https://raw.githubusercontent.com/w3c/wcag/main/guidelines/sc/20/focus-visible.html)) |
| WCAG-2.4.13 | should | Make the focus indicator at least as large as a 2 CSS-pixel perimeter of the control and change those pixels by at least 3:1 between focused and unfocused states. | outline-width >= 2 CSS px on all four sides (unclipped) and >= 3:1 change; 0 clipped rings. AAA in WCAG 2.2, so scored as should. | primary ([source](https://raw.githubusercontent.com/w3c/wcag/main/guidelines/sc/22/focus-appearance.html)) |
| WCAG-2.4.11 | must | Never let the sticky top bar, the andon strip, the fixed overlay or any other author-positioned content completely cover the element that has keyboard focus. | Minimum (AA): 0 focused elements 100% covered. Enhanced (AAA, should): 0 focused elements partially covered. | primary ([source](https://raw.githubusercontent.com/w3c/wcag/main/guidelines/sc/22/focus-not-obscured-minimum.html)) |
| WCAG-2.5.8 | must | Make every pointer target at least 24x24 CSS pixels, or space undersized targets so 24px circles centred on them do not touch another target. | 0 interactive targets under 24x24 CSS px that also fail the spacing test. | primary ([source](https://raw.githubusercontent.com/w3c/wcag/main/guidelines/sc/22/target-size-minimum.html)) |
| WCAG-2.5.5 | should | On operatory, shared and phone profiles make every pointer target at least 44x44 CSS pixels — the prototype's own --target token — because gloved fingers on tablets need it. | 100% of interactive targets >= 44x44 on operatory, shared and phone profiles; >= 95% on desk with the remainder >= 24x24. AAA in WCAG, house rule here. | primary ([source](https://raw.githubusercontent.com/w3c/wcag/main/guidelines/sc/21/target-size-enhanced.html)) |
| WCAG-2.5.7 | must | Anything a user can do by dragging (reordering the board, moving a chair, sliding a value, scrubbing the perio pad) must also be doable with single taps or clicks. | 0 functions operable only by dragging. | primary ([source](https://raw.githubusercontent.com/w3c/wcag/main/understanding/22/dragging-movements.html)) |
| WCAG-3.2.3 | must | Keep the top-bar navigation, rail tabs and screen-level tab strips in the same relative order on every screen for a given role and device profile. | 0 order inversions across screens for the same role and profile; items may be absent but never reordered unless the user reordered them. | primary ([source](https://raw.githubusercontent.com/w3c/wcag/main/guidelines/sc/20/consistent-navigation.html)) |
| WCAG-3.2.6 | must | Put the help / support control in the same position relative to other page content on every screen where it appears. | Same relative DOM order on 100% of screens where the help mechanism appears; 0 screens where it moves to a different container. | primary ([source](https://raw.githubusercontent.com/w3c/wcag/main/guidelines/sc/22/consistent-help.html)) |
| WCAG-3.3.1 | must | When input is rejected, name the field in error and say what is wrong in text next to it, not only with colour or a shake. | 100% of automatically detected input errors produce visible text that identifies the item; 0 errors signalled by colour or border change alone. | primary ([source](https://raw.githubusercontent.com/w3c/wcag/main/guidelines/sc/20/error-identification.html)) |
| WCAG-3.3.2 | must | Give every input a visible, persistent label or instruction — a placeholder alone is not a label. | 100% of fields have a visible persistent label; 0 fields labelled by placeholder or aria-label alone. | primary ([source](https://raw.githubusercontent.com/w3c/wcag/main/guidelines/sc/20/labels-or-instructions.html)) |
| WCAG-3.3.3 | must | When the fix is known, tell the user what to enter or do next (the allowed range, the missing field, the control that clears the gate) in the same message. | 100% of refusals have a control or a Why explanation; 100% of validation errors with a knowable fix state it; verb line <= 12 words (prototype convention, judgement). | primary ([source](https://raw.githubusercontent.com/w3c/wcag/main/guidelines/sc/20/error-suggestion.html)) |
| WCAG-3.3.4 | must | Every action that posts money, voids, deletes or closes the day must be reversible, checked, or confirmed on a review step that shows what is about to happen — and routine saves must NOT be burdened with confirmations. | 100% of money-moving and data-destroying actions satisfy at least one of reversible / checked / confirmed; confirmation dialogs on reversible actions = 0 (the understanding document says each simple save does not need confirmation). | primary ([source](https://raw.githubusercontent.com/w3c/wcag/main/guidelines/sc/20/error-prevention-legal-financial-data.html)) |
| WCAG-2.2.1 | must | Any time limit — auto-lock on a shared tablet, auto-dismissing toast or banner, session expiry — must be switchable off, adjustable to 10x, or warned with 20 seconds to extend. | 0 time limits under 20 hours without a turn-off, a >=10x adjustment, or a 20-second warning with >=10 extensions. | primary ([source](https://raw.githubusercontent.com/w3c/wcag/main/guidelines/sc/20/timing-adjustable.html)) |
| WCAG-2.2.2 | must | Nothing may move, blink or auto-update for more than five seconds alongside other content unless it can be paused, and with reduced motion every animation must stop. | 0 automatic animations lasting > 5s without a pause control; under reduced motion 0 running animations and all transition-duration = 0s. | primary ([source](https://raw.githubusercontent.com/w3c/wcag/main/guidelines/sc/20/pause-stop-hide.html)) |

### What automated testing proves, and the scripted checks for the rest

| Id | Level | Heuristic | Passes when | Evidence |
|---|---|---|---|---|
| AXE-RUN-all-contexts | must | Run axe-core 4.13.0 with the tag set wcag2a, wcag2aa, wcag21a, wcag21aa, wcag22aa, best-practice over every route, theme and width, and accept only zero violations of any impact. | 0 violations across all contexts (currently the script tolerates moderate/minor; the audit must not). 0 contexts with an error (an errored context is a missing measurement, never a pass). | primary ([source](https://raw.githubusercontent.com/dequelabs/axe-core/develop/doc/API.md)) |
| AXE-incomplete-never-pass | must | Treat every axe 'incomplete' (needs-review) node as an open question that a scripted or manual check must close; never count it as a pass. | 0 untriaged incomplete nodes per context; the report lists an explicit disposition (excused-hidden-glyph \| resolved-by-script \| manual-pass \| manual-fail) for 100% of incomplete nodes. The phrase 'zero violations' alone is not a pass statement for any rule that returned incomplete nodes. | primary ([source](https://raw.githubusercontent.com/dequelabs/axe-core/develop/doc/API.md)) |
| AXE-4.1.2-name-role-value | must | Every control exposes a name, a role and its state to assistive technology: run the axe 4.1.2 rule cluster and accept zero violations and zero incomplete nodes. | 0 violations and 0 incomplete nodes for the listed rules in every context; 0 aria-pressed values other than 'true'/'false'. | primary ([source](https://raw.githubusercontent.com/dequelabs/axe-core/develop/lib/rules/aria-prohibited-attr.json)) |
| AXE-1.3.1-tables-resolved | must | Every data table has column headers that describe real data cells, proven by the axe table rules plus a scripted header/cell check for the table axe could not decide. | 0 violations; 0 incomplete for th-has-data-cells after the scripted check; 100% of th carry scope; 100% of header columns have >= 1 non-empty td; 100% of tables have a name. | primary ([source](https://raw.githubusercontent.com/dequelabs/axe-core/develop/lib/rules/th-has-data-cells.json)) |
| AXE-2.4.1-bypass-verified | must | The skip link is the first Tab stop on every route and actually moves focus into the work canvas. | 100% of 16 routes at 1280, 1024 and 420: first Tab = skip link, visible; Enter lands focus in #canvas. axe bypass/skip-link: 0 violations. | primary ([source](https://raw.githubusercontent.com/w3c/wcag/main/guidelines/sc/20/bypass-blocks.html)) |
| WCAG-1.4.10-reflow-320 | must | Every screen works at 320 CSS px wide with vertical scrolling only, except data tables and the odontogram which may scroll sideways inside their own region. | 0 px horizontal page overflow on 100% of routes; 2-D scrollers only among the 4 allowed table/chart wrappers, each focusable and named; testid set difference = empty. | primary ([source](https://raw.githubusercontent.com/w3c/wcag/main/guidelines/sc/21/reflow.html)) |
| WCAG-2.4.3-focus-order | must | Tab moves through each screen in the order a person reads it, enters an opened dialog first and returns to the opener when the dialog closes. | 100% of stops in DOM order; >= 98% in visual order with each exception listed; 100% of dialog open/close cycles land focus as specified; 0 cases of activeElement === document.body after any action. | primary ([source](https://raw.githubusercontent.com/w3c/wcag/main/guidelines/sc/20/focus-order.html)) |
| WCAG-1.3.2-meaningful-sequence | must | The DOM order is the reading order on every screen and at every breakpoint; CSS grid areas and flex order never rearrange content. | 0 elements with non-zero order; DOM order == visual order for 100% of grid-area children at every breakpoint; 100% of live texts present as visible text. | primary ([source](https://raw.githubusercontent.com/w3c/wcag/main/guidelines/sc/20/meaningful-sequence.html)) |
| WCAG-2.1.1-keyboard-custom-widgets | must | Every flow in scripts/lib/flows.mjs can be completed with the keyboard alone, and every custom widget answers the standard keys. | 5 of 5 flows complete keyboard-only within the flow's budgetKeystrokes where defined; 100% of clickable testids in the sequential focus order; 100% of listed key contracts pass. | primary ([source](https://raw.githubusercontent.com/w3c/wcag/main/guidelines/sc/20/keyboard.html)) |
| WCAG-4.1.3-status-messages | must | Every state change that does not move focus is announced once through a live region that already exists at page load, and re-renders never announce chips. | 100% of focus-keeping state changes produce exactly one #live announcement within 300 ms; <= 2 live-region mutations per action; 0 assertive regions outside stop-severity; 0 duplicate announcements of the same gate. | primary ([source](https://raw.githubusercontent.com/w3c/wcag/main/guidelines/sc/21/status-messages.html)) |
| WCAG-3.2.4-consistent-identification | must | The same function carries the same label, the same button identity and the same status word on every screen. | 0 testid suffixes with > 1 label; 0 labels mapped to > 1 identity; 0 chip words outside the shared tables; 0 support-line wordings other than SUPPORT. | primary ([source](https://raw.githubusercontent.com/w3c/wcag/main/guidelines/sc/20/consistent-identification.html)) |
| WCAG-3.3.7-redundant-entry | must | Within one process nobody re-types what the system already knows: patient, provider, amount owed, date and tooth are pre-filled or selectable. | 0 non-security fields asking again for a value already known in the same process; the checkout amount field is pre-filled with the patient portion; the note tooth number matches the charted tooth without typing. | primary ([source](https://raw.githubusercontent.com/w3c/wcag/main/guidelines/sc/22/redundant-entry.html)) |
| WCAG-2.5.3-label-in-name | should | A control's accessible name begins with the words printed on it, so voice users can say what they see. | 0 violations of label-content-name-mismatch; 100% of controls whose accessible name contains the visible text, >= 95% starting with it, each exception listed. | primary ([source](https://raw.githubusercontent.com/w3c/wcag/main/guidelines/sc/21/label-in-name.html)) |

### Cognitive load theory and the HCI laws

| Id | Level | Heuristic | Passes when | Evidence |
|---|---|---|---|---|
| CLT-chunk-4 | should | Keep each card or control group to at most four visible controls, never more than seven, so a gloved user can hold the whole group in working memory. | controlsPerCardMax <= 4 passes (Cowan 4±1); 5-7 is a warning (Miller 7±2); > 7 fails. Baseline: Board 8, Roles 11, Checkout 10, Risk 8 fail; Encounter 40 is exempt only if the odontogram is marked role=grid. | unverified |
| CLT-hick-7 | should | Offer no more than seven visible options in any one selection group; beyond that, split the group, put the rest behind a labelled More, or provide typed search. | <= 7 visible options per group passes; 8-15 requires a search/filter input in the same container; > 15 fails outright. Baseline: sign-in shows 8 persona tiles (fails by one); Board topbar nav 11 (fails). | secondary ([source](https://raw.githubusercontent.com/uswds/uswds-site/main/_components/select/guidance/when-to-use.md)) |
| CLT-topbar-7 | must | Keep the persistent top bar to seven or fewer controls that fit on at most two rows at every supported width. | topbarControls <= 7 and topbarRows <= 2 at every width passes; 8-9 controls is a warning; >= 10 controls or >= 3 rows fails. Baseline: Ledger 19 controls / 11 rows desk and 10 rows phone; Close and Risk 14 controls / 5-7 rows; Board 11 controls / 3-4 rows all fail. | secondary ([source](https://raw.githubusercontent.com/uswds/uswds-site/main/_components/button/guidance/usability.md)) |
| CLT-fitts-target | must | Make every interactive target at least 44 by 44 px and place an item's action inside the same card as the item it acts on. | 100% of interactive targets >= 44 x 44 px (gloves on a shared tablet); 100% of per-item actions co-located with their item. Static .chip (28px) passes only if it is not focusable; a focusable chip must be .chip.big. | secondary ([source](https://raw.githubusercontent.com/ant-design/ant-design/master/docs/spec/lightweight.en-US.md)) |
| CLT-recognition-keys | should | Show every keyboard shortcut on the control it triggers, never only in a legend the user has to remember. | 0 legend-only shortcuts per screen. Any shortcut that exists only in the .sub legend fails; the legend may stay as a duplicate but not as the sole source. | unverified |
| CLT-disclosure-depth | must | Hide only what some users need behind a single level of disclosure; anything needed to finish the task stays visible. | Max disclosure depth 2; 0 finish-critical elements hidden by default; at most 1 'More' toggle per card. The refusal 'Why' details (depth 1) passes. | secondary ([source](https://raw.githubusercontent.com/alphagov/govuk-design-system/main/src/components/details/index.md)) |
| CLT-prose-budget | should | Put no more than 40 words of prose between a region's heading and its finish button, and no more than 120 words of prose above the fold on any screen. | proseBeforeFinishMax <= 40 and proseWordsAboveFold <= 120 pass. Baseline failures: Money 94 before finish, Close 73, Phone 64, Ledger 48; above fold Risk 187, Close 153, Phone 149, Ledger 134, Money 129. | secondary ([source](https://raw.githubusercontent.com/Shopify/polaris/main/polaris.shopify.com/content/content/fundamentals.mdx)) |
| CLT-label-words | should | Keep visible button labels to four words or fewer and accessible names to twelve words or fewer. | Visible label <= 4 words: 100% pass; accessible name <= 12 words: 100% pass. Baseline: Chairs 22-word summary label, Close 14, Phone 8 and sign-in 7 fail the visible rule; Board 13-word accessible name fails the name rule. | secondary ([source](https://raw.githubusercontent.com/uswds/uswds-site/main/_components/button/guidance/usability.md)) |
| CLT-chip-vocab | should | Use at most twelve distinct chip labels per screen and at most three chips per card, or the chips stop signalling anything. | chipVocabulary <= 12 and chips per card <= 3 pass. Baseline: Board and Temp Board 29 distinct labels across 105 chips fail; Roles 14 fails; Chairs 12 passes on vocabulary. | secondary ([source](https://raw.githubusercontent.com/uswds/uswds-site/main/_components/tag/guidance/usability.md)) |
| CLT-chip-not-button | must | Things that look alike must behave alike: a chip is never clickable, and anything clickable looks like a button. | 0 interactive .chip elements and 0 focusable elements without a button identity class. Any interactive chip must be re-classed as .btn.compact. | secondary ([source](https://raw.githubusercontent.com/uswds/uswds-site/main/_components/tag/guidance/usability.md)) |
| CLT-proximity-ratio | should | Space between groups must be at least twice the space within a group, so the eye reads cards as units without borders doing all the work. | Ratio >= 2 (Ant Design tiers 8 / 16 / 24) passes; ratio < 2 fails. Baseline: .worklist gap 8px equals .actions gap 8px (ratio 1) on Board, Money and Ledger lists, failing. | secondary ([source](https://raw.githubusercontent.com/ant-design/ant-design/master/docs/spec/proximity.en-US.md)) |
| CLT-common-region | must | Give every group of three or more controls a visible group name inside the same bounded region. | 0 unlabelled groups of >= 3 controls per screen. A group whose only label is a distant page-head .sub fails. | primary ([source](https://raw.githubusercontent.com/w3c/wai-tutorials/master/source/forms/grouping.html.erb.md)) |
| CLT-similarity-identity | must | Use the same visual identity for the same kind of action everywhere: no more than four button identities in the product and zero one-off styles. | byIdentity.other === 0 on every screen and <= 4 distinct identity tuples product-wide (excluding aria-pressed/aria-current states). Baseline: Board 4 'other', Money 1, Chairs 1, Close/Checkout 1 fail. | secondary ([source](https://raw.githubusercontent.com/ant-design/ant-design/master/docs/spec/repetition.en-US.md)) |
| CLT-jakob-nav | must | Keep the top bar identical in item order and labels on every screen, with brand top-left, search next, and the current screen marked. | Identical nav label sequence on 100% of screens for the same persona; .brand is the first child and search the second control on every screen; exactly one aria-current=page per screen. Any reordering, renaming or missing marker fails. | secondary ([source](https://raw.githubusercontent.com/ant-design/ant-design/master/docs/spec/values.en-US.md)) |
| CLT-tesler-defaults | should | Let the system carry the irreducible complexity: every choice on a finish path has the most common answer preselected so the user only confirms or overrides. | 0 empty required choices on load per finish path; 0 fields requiring re-entry of data already shown on the same screen. A choice that is deliberately neutral for a prudent decision (see CLT-neutral-irreversible) is exempt when documented. | secondary ([source](https://raw.githubusercontent.com/uswds/uswds-site/main/_components/select/guidance/usability.md)) |
| CLT-serial-position | should | Put the most-used item first and the finish action last in every list, row or region, because first and last positions are remembered and found best. | 100% of finish regions end with the finish button; 100% of persona navs start with the landing screen; >= 90% of card action rows lead with the advancing action. | unverified |
| CLT-split-attention | should | Place every explanation, hint or legend in the same bounded container as the thing it explains, so the eye never has to travel to integrate them. | 0 hints whose referent lives in a different container or > 200 px away. Baseline: the 'keys: …' legends in page-head .sub refer to buttons in cards 300-2000 px below, failing; the refusal 'Why' text inside .refusal passes. | unverified |
| CLT-redundancy | should | Say each thing once per view: no string longer than three words may repeat verbatim across half or more of the cards on a screen. | 0 non-action strings > 3 words repeated across >= 50% of cards; 0 chip/text duplicates within one card. Baseline: Chairs repeats 'Since last visit No changes since last visit More' on 5 of 6 cards, failing. | secondary ([source](https://raw.githubusercontent.com/uswds/uswds-site/main/_components/card/guidance/usability.md)) |
| CLT-scent-heading | should | Make every navigation label reappear in the destination's h1, keep the h1 to five words, and keep it unique per screen. | 100% nav labels share a word with their destination h1; h1Words <= 5; h1 unique across screens. Baseline: Board and Temp Board h1 'Board · Main Street · Thursday 9/3' is 7 words and duplicated, failing; 'Money Desk', 'Chairs · mine' pass. | secondary ([source](https://raw.githubusercontent.com/alphagov/govuk-design-system/main/src/patterns/question-pages/index.md)) |
| CLT-one-task | should | Give each finish-path screen one task: one h1, one finish button, and no more than four section headings above the fold. | Exactly 1 h1; exactly 1 finish button on finish-path screens; <= 4 headings above the fold; <= 5 top-level regions above the fold. Baseline: Board 7 headings, Encounter 6, Close 5 fail the heading rule. | secondary ([source](https://raw.githubusercontent.com/alphagov/govuk-design-system/main/src/patterns/question-pages/index.md)) |
| CLT-reading-grade | should | Write all visible prose at or below an 8th-grade reading level so it can be read in one glance between patients. | Flesch-Kincaid grade <= 8.0 per screen and 0 sentences over 25 words pass; grade 8-10 warning; > 10 fails. | secondary ([source](https://raw.githubusercontent.com/Shopify/polaris/main/polaris.shopify.com/content/content/fundamentals.mdx)) |
| CLT-neutral-irreversible | must | For an irreversible money or clinical action, stay neutral: never autofocus or preselect it, and always place an equal-size reversible alternative beside it. | 100% of irreversible buttons: not focused on load, have an adjacent reversible alternative of equal height; 0 irreversible buttons alone in a row. | secondary ([source](https://raw.githubusercontent.com/ant-design/ant-design/master/docs/spec/contrast.en-US.md)) |

### Nielsen, Norman and the evidence on intuitive interfaces

| Id | Level | Heuristic | Passes when | Evidence |
|---|---|---|---|---|
| INT-visible-acknowledgement | must | Every press is visibly acknowledged on screen within one second so the person never wonders whether it took. | 100% of primary presses show a text or identity change within 1000 ms; 0 presses acknowledged by colour alone | unverified |
| INT-one-h1-names-place | must | Every screen has exactly one h1 that names the place, is unique to that screen, and is not the persona's section name. | exactly 1 h1 per screen; 0 duplicate h1 across routes of one persona; 0 generic/persona-only h1; ≤ 5 words before the dot-separated context segment | secondary ([source](https://raw.githubusercontent.com/alphagov/govuk-design-system/main/src/patterns/question-pages/index.md)) |
| INT-verb-labels | must | Every button label starts with the verb that names the action it performs, in sentence case. | ≥ 95% of action buttons start with a verb; 100% sentence case; 0 labels that are 'OK', 'Yes', 'No' or 'Submit' | secondary ([source](https://raw.githubusercontent.com/alphagov/govuk-design-system/main/src/components/button/index.md)) |
| INT-one-primary-per-view | must | Each view shows at most one primary (irreversible or held) call to action in the first screenful, so the next step is never ambiguous. | ≤ 1 page-level irreversible/held button per view and per dialog; ≤ 3 page-level reversible buttons in the first screenful | secondary ([source](https://raw.githubusercontent.com/nhsuk/nhsuk-service-manual/main/app/views/design-system/components/buttons/index.njk)) |
| INT-irreversible-identity | must | Every action that cannot be undone wears the irreversible identity, is confirmed in a second step, and is never signalled by colour alone. | 100% of irreversible mutations behind .btn.irreversible; 100% have a two-step confirm; 0 identity pairs distinguishable by hue only | secondary ([source](https://raw.githubusercontent.com/alphagov/govuk-design-system/main/src/components/button/index.md)) |
| INT-no-double-submit | must | A double press on a primary control never performs the action twice. | 0 duplicate mutations across all primary controls under both dblclick and two-clicks-in-300 ms | secondary ([source](https://raw.githubusercontent.com/alphagov/govuk-design-system/main/src/components/button/index.md)) |
| INT-no-disabled-use-held | should | A primary that cannot act yet is shown as Held with a visible line saying what to do next, never as a greyed-out disabled button. | 0 button[disabled] on any screen except inside a dialog that is itself in progress; 100% of .btn.held have an adjacent visible reason | secondary ([source](https://raw.githubusercontent.com/nhsuk/nhsuk-service-manual/main/app/views/design-system/components/buttons/index.njk)) |
| INT-error-wording | must | Refusal and error text is plain, specific and free of apology, blame, jargon and codes. | 0 regex hits; 0 generic messages; 100% of verb lines ≤ 12 words; 100% of Why texts ≤ 40 words | secondary ([source](https://raw.githubusercontent.com/alphagov/govuk-design-system/main/src/components/error-message/index.md)) |
| INT-keep-data-and-gate-on-press | must | A refusal never erases what the person typed, and validation happens when they press, not when they leave a field. | 0 fields cleared by a refusal; 0 refusals raised on blur; focus on refusal or field in 100% of cases | secondary ([source](https://raw.githubusercontent.com/alphagov/govuk-design-system/main/src/components/error-message/index.md)) |
| INT-labels-visible-above | must | Every input has a visible label placed above it, and no input relies on placeholder text as its label. | 100% of inputs labelled by visible text; 100% labels above (except check/radio); 0 placeholder-only labels; 0 trailing colons | secondary ([source](https://raw.githubusercontent.com/alphagov/govuk-design-system/main/src/components/text-input/index.md)) |
| INT-instructions-before-input | should | Required fields, expected formats and constraints are stated before the person types, not discovered through refusal. | 100% of required fields marked before submit; 0 constraints revealed only in a refusal | primary ([source](https://raw.githubusercontent.com/w3c/wai-tutorials/master-2.0/content/forms/instructions.md)) |
| INT-on-input-no-surprise | must | Changing a select, checkbox, toggle or radio never navigates, submits or opens a dialog unless the control says it will. | 0 selects/checkboxes/toggles that change route, open a dialog or commit a record on change | primary ([source](https://raw.githubusercontent.com/w3c/wcag/main/guidelines/sc/20/on-input.html)) |
| INT-success-says-what-next | should | When something the person was expecting happens, the screen confirms it in words and says what happens next. | 100% of commits confirmed in text with a next step; 0 confirmations that persist across a route change | secondary ([source](https://raw.githubusercontent.com/alphagov/govuk-design-system/main/src/patterns/confirmation-pages/index.md)) |
| INT-real-world-words | must | The screen speaks the clinic's language: every code, abbreviation or system term is paired with the word staff actually use. | 0 bare codes without a word; 0 raw store keys, ISO timestamps, 24-hour times or unformatted cents visible on any screen | unverified |
| INT-exit-and-undo | must | Every dialog and every reversible action offers a clearly marked way out — Escape or Cancel to leave, Undo to take it back. | 100% of dialogs close on Escape and have a visible Cancel; focus returns to opener in 100%; ≥ 95% of reversible actions have an on-screen inverse | unverified |
| INT-recognition-not-recall | must | Choices are shown, not remembered: the person picks from what is on screen and never has to hold a code, name or prior screen in their head. | 0 free-text inputs for enumerable values; 0 glyph-only chips; 100% of dialogs restate their object | unverified |
| INT-temp-first-screenful | must | A first-day temp can state the screen's purpose and its first action from the first screenful alone, because that screenful holds only what the task needs. | 2 of 2 reviewers agree on purpose and first action for 100% of routes at 1280 and ≥ 90% at 420; ≤ 14 interactive elements in the first screenful (≤ 20 on Board/Chairs where each row carries one action); ≤ 3 severity fills at once; 0 decorative-only elements | unverified |
| INT-signifiers | must | Everything that can be pressed looks pressable, and nothing that looks pressable is inert. | 0 non-control elements with handlers; 0 inert elements styled as buttons; 0 controls < 44×44 px | unverified |

### What the NHS, GOV.UK and USWDS design systems settled

| Id | Level | Heuristic | Passes when | Evidence |
|---|---|---|---|---|
| CDS-TYPE-body-16 | must | Set body text to at least 16px; the three systems settle at 16px (USWDS, NHS mobile) or 19px (GOV.UK, NHS tablet+). | Body computed font-size >= 16px; 0 running-text paragraphs (p, li, dd, .sentence, .explain) below 16px. 15px fails. | primary ([source](https://raw.githubusercontent.com/uswds/uswds/develop/packages/uswds-core/src/styles/settings/_settings-typography.scss)) |
| CDS-TYPE-heading-scale | should | Use a short bold heading scale where each step is clearly larger than the next (GOV.UK 48/36/24/19, NHS 48/36/26/22/19, all bold) and page h1 is at least 24px. | h1 >= 24px; all headings font-weight >= 600; each adjacent ratio >= 1.15 (prototype h3/h4 = 1.13 fails); at most 5 heading sizes in use. | primary ([source](https://raw.githubusercontent.com/alphagov/govuk-frontend/main/packages/govuk-frontend/src/govuk/core/_typography.mixin.scss)) |
| CDS-TYPE-two-weights | should | Use exactly two font weights: one regular (400) and one bold (600 NHS, 700 GOV.UK/USWDS); do not introduce medium or semi-bold in-betweens. | Distinct computed weights <= 2 and the bold one is 600 or 700; any 500 occurrence fails. | primary ([source](https://raw.githubusercontent.com/nhsuk/nhsuk-frontend/main/packages/nhsuk-frontend/src/nhsuk/core/settings/_globals.scss)) |
| CDS-SPACE-scale-4-8 | must | Draw every padding, margin and gap from one small spacing scale based on 4/8px (NHS 0,4,8,16,24,32,40,48; USWDS 4,8,12,16,20,24,32); off-scale values like 6px or 10px are not allowed. | >= 95% of declared spacing values on the scale and 0 off-scale values on shared components (.btn, .chip, .card, .field, td). | primary ([source](https://raw.githubusercontent.com/nhsuk/nhsuk-frontend/main/packages/nhsuk-frontend/src/nhsuk/core/settings/_spacing.scss)) |
| CDS-BTN-border-2px | should | Give buttons and form controls one 2px border so outline and filled identities share the same footprint (GOV.UK and NHS form-element border width is 2px). | border-width == 2px on 100% of buttons and inputs; height difference between adjacent filled and outline buttons == 0px. | primary ([source](https://raw.githubusercontent.com/alphagov/govuk-frontend/main/packages/govuk-frontend/src/govuk/settings/_measurements.scss)) |
| CDS-BTN-text-bold-body | should | Set button labels in the bold weight at the body text size with line-height 1 (NHS button: 19px bold, line-height 19px); never smaller than body text. | font-weight >= 600 and font-size >= body font-size on 100% of buttons; no button label wraps to a second line at 1024px. | primary ([source](https://raw.githubusercontent.com/nhsuk/nhsuk-frontend/main/packages/nhsuk-frontend/src/nhsuk/components/button/_index.scss)) |
| CDS-ERR-summary-top | must | When a submit fails, show one error summary before the form: a heading, a role=alert wrapper, and one link per error that moves focus to its field; the summary receives focus on appearance. | Present on 100% of forms with >= 1 invalid field; links == errors; summary focused within 500ms of submit; summary border >= 4px in the error colour. | primary ([source](https://raw.githubusercontent.com/alphagov/govuk-frontend/main/packages/govuk-frontend/src/govuk/components/error-summary/template.njk)) |
| CDS-ERR-message-prefix | must | Place each field's error message between the label/hint and the input, in bold error colour, and start it with a visually hidden 'Error:' prefix so screen readers announce the state. | 100% of invalid fields have a preceding, described-by message with the hidden prefix; 0 messages placed only below the input or only as border colour. | primary ([source](https://raw.githubusercontent.com/nhsuk/nhsuk-frontend/main/packages/nhsuk-frontend/src/nhsuk/components/error-message/template.njk)) |
| CDS-COLOUR-functional-roles | must | Route every colour through a named functional role (GOV.UK defines about 20: brand, text, secondary-text, focus, focus-text, error, success, border, input-border, hover, link states, backgrounds); components never use raw hex. | 0 raw colour literals outside tokens.css; <= 24 semantic roles (prototype ~22) plus the severity ladder; 100% of sampled computed colours resolve to a declared token. | primary ([source](https://raw.githubusercontent.com/alphagov/govuk-frontend/main/packages/govuk-frontend/src/govuk/settings/_colours-functional.scss)) |
| CDS-STATUS-roles-max | should | Keep status semantics to at most five or six named roles (USWDS: error, warning, success, info, emergency; GOV.UK tags add neutral grey), each with a light background/dark ink pair; more steps than a user can name is noise. | <= 6 severity roles total (prototype has exactly 6: stop, required, review, style, info, clear); <= 4 distinct severities on one screen; soft/ink contrast >= 4.5:1 in both themes; luminance strictly monotonic stop < required < review < style < info < clear in grayscale. | primary ([source](https://raw.githubusercontent.com/uswds/uswds/develop/packages/uswds-core/src/styles/settings/_settings-color.scss)) |
| CDS-TAG-tint-shade | should | Colour a status tag with a dark shade of one hue for text on a light tint of the same hue (GOV.UK: shade-50 text on tint-80 background), bold, at least 13px, with 8px horizontal padding, and never make it look clickable. | font-size >= 13px; font-weight >= 600; text/background contrast >= 4.5:1 in both themes; padding-x == 8px (prototype 10px fails scale); 0 non-interactive chips with cursor pointer. | primary ([source](https://raw.githubusercontent.com/alphagov/govuk-frontend/main/packages/govuk-frontend/src/govuk/components/tag/_mixin.scss)) |
| CDS-TABLE-density | should | Use one table density: cells padded 8-12px vertically and 16-24px to the right, a 1px bottom rule, top-aligned left-aligned text, bold headers, and body text at full size (GOV.UK 10px 20px 10px 0; NHS compact 4/8px only for dense data). | padding-top in [8,12]px, padding-right in [16,24]px, border-bottom 1px, th font-weight >= 600, td font-size >= body size, row height >= 36px; <= 2 distinct density presets across the prototype. | primary ([source](https://raw.githubusercontent.com/alphagov/govuk-frontend/main/packages/govuk-frontend/src/govuk/components/table/_mixin.scss)) |
| CDS-DETAILS-disclosure | should | Hide secondary detail behind a native details/summary (or button[aria-expanded]) whose summary reads like a link, is at least 44px tall, and shrink-wraps its text so the focus ring hugs the label. | 100% of collapsible regions have a keyboard-operable toggle with correct expanded state; toggle height >= 44px; 0 disclosures implemented as plain div click handlers. | primary ([source](https://raw.githubusercontent.com/alphagov/govuk-frontend/main/packages/govuk-frontend/src/govuk/components/details/_mixin.scss)) |
| CDS-WARNING-text-icon | should | Before an irreversible or legally significant action, show bold warning text with a large non-colour icon (GOV.UK: 35px black circle with '!') so the warning survives grayscale and forced colours. | 100% of irreversible confirms carry bold text plus an icon >= 24px; icon contrast >= 3:1 in grayscale. | primary ([source](https://raw.githubusercontent.com/alphagov/govuk-frontend/main/packages/govuk-frontend/src/govuk/components/warning-text/_mixin.scss)) |
| CDS-LABEL-hint-body-size | should | Set form labels and hint text at the body font size in the secondary text colour for hints (NHS label and hint are both 19px, hint in grey-1 #4c6272); never shrink labels to a caption size. | label font-size >= body font-size (13px vs 15px fails); hint font-size >= body font-size; hint contrast >= 4.5:1 (NHS grey-1 on white is ~7:1). | primary ([source](https://raw.githubusercontent.com/nhsuk/nhsuk-frontend/main/packages/nhsuk-frontend/src/nhsuk/components/hint/_index.scss)) |
| CDS-ICON-size-24 | should | Render functional icons and glyphs at a fixed 24px (NHS default; 20px small, 32px large) rather than as small typographic characters. | Icon box >= 20px on buttons and >= 16px on chips; 0 meaning-carrying glyphs under 16px (prototype 11px glyphs fail). | primary ([source](https://raw.githubusercontent.com/nhsuk/nhsuk-frontend/main/packages/nhsuk-frontend/src/nhsuk/core/settings/_globals.scss)) |

### Typography, spacing and colour for readability

| Id | Level | Heuristic | Passes when | Evidence |
|---|---|---|---|---|
| TYPE-min-any-text-12 | must | Never render readable text below 12px; decorative glyphs may be smaller only if the same information is also given in text of 12px or more. | Zero text nodes < 12px. Prototype has .psite .ghost 10px (168 instances on perio), .psite.bleed::after 9px, .psite.sup::before 9px, .chip .glyph 11px, .count 12px; the 9 and 10px cases fail unless the value is duplicated in >= 12px text. | secondary ([source](https://raw.githubusercontent.com/Myndex/SAPC-APCA/master/documentation/APCA_in_a_Nutshell.md)) |
| TYPE-line-height-1.5 | should | Give multi-line prose a line-height of at least 1.5 times its font size; keep headings and single-line controls at 1.1 to 1.3. | Wrapping prose ratio >= 1.5 (prototype body is 1.45 and .rail-msg/.rail-sum .body are 1.4, so they fail; .explain .sentence 1.6 and .md-* 1.5 pass). Headings 1.1 to 1.3. | primary ([source](https://raw.githubusercontent.com/w3c/wcag/main/guidelines/sc/20/visual-presentation.html)) |
| TYPE-measure-80ch | should | Cap prose line length at 80 characters (aim for 45 to 75) by giving every prose container a max-width in ch or em. | chars <= 80 for every prose element (must, WCAG); chars <= 75 for body prose (should). At 1280px the .canvas is ~928px wide and most pages set no max-width (only .co-page/.md-page/.dc-page/.rl-page/.ledger-page 1100px, .signin 760px), giving ~130 chars at 15px Inter — fails. NHS uses max-width 44em, USWDS measure 4 = 68ex. | primary ([source](https://raw.githubusercontent.com/w3c/wcag/main/guidelines/sc/20/visual-presentation.html)) |
| TYPE-scale-no-offscale | should | Draw every font size from the six-step token scale and use no more than five distinct sizes on one screen; delete the hard-coded 9, 10, 11, 12 and 14px sizes. | Zero off-scale declarations (components.css currently has 11 at 9/10/11/12/13/14/15px); zero off-scale computed sizes; <= 5 distinct sizes per screen (baseline: board 5 incl. 11px, money 6, encounter 7, perio 6 incl. 10px). | primary ([source](https://raw.githubusercontent.com/alphagov/govuk-frontend/main/packages/govuk-frontend/src/govuk/helpers/_typography.scss)) |
| TYPE-tabular-money | should | Render every money amount, time, count and ID with tabular numerals and right-align money in columns so digits line up. | 100% of money strings tabular and right-aligned; 100% of times and counts tabular. Prototype sets tabular-nums on .num, .mono, .amt, .count, td.num, .co-amount, .md-*, .ph-amount; audit catches any amount rendered in a plain span. | primary ([source](https://raw.githubusercontent.com/alphagov/govuk-frontend/main/packages/govuk-frontend/src/govuk/helpers/_typography.scss)) |
| TYPE-uppercase-tracking | should | Use all-caps only for labels of three words or fewer, and add letter-spacing of at least 0.04em when you do; never set running text in uppercase. | letter-spacing / font-size >= 0.04 and word count <= 3 for every uppercase element; zero uppercase elements with > 3 words. Prototype .pal-groups (uppercase, .04em) passes. | unverified |
| TYPE-no-justify-left-align | must | Left-align all prose and never justify it. | Zero justified elements; zero centred or right-aligned multi-line prose. Prototype has .pindots and .tile .glyph centred (single-line) which pass. | primary ([source](https://raw.githubusercontent.com/w3c/wcag/main/guidelines/sc/20/visual-presentation.html)) |
| TYPE-dyslexia-sans-no-italic | should | Use a sans-serif face throughout and emphasise with bold, never with italics or underline (underline is reserved for links). | Body font resolves to a sans-serif (Inter / Segoe UI / system-ui passes); zero italic text elements; zero underlined non-link text. | unverified |
| WCAG-1.4.3-placeholder-hover-focus | must | Hold placeholder, hover-state and focus-state text to the same 4.5:1 minimum as resting text. | >= 4.5:1 in all three states, both themes; zero regressions between resting and hover/focus state. | primary ([source](https://raw.githubusercontent.com/w3c/wcag/main/understanding/20/contrast-minimum.html)) |
| COLOR-roles-per-screen | should | Limit each screen to at most seven distinct text colours and six distinct background colours so colour stays a small, learnable vocabulary. | textColours <= 7 and backgroundColours <= 6 per screen. Baseline: board 11/9, temp-board 11/10, close 11/7, money 8/6, chairs 8/7 fail; signin 3/3, exams 3/3, perio 5/4, encounter 5/4 pass. | unverified |
| COLOR-dark-theme-not-inverted | should | Build the dark theme as its own palette: surfaces step lighter with elevation and are never pure black, body text is never pure white, severity inks are lightened and desaturated, and every WCAG ratio is re-measured in dark. | L(--bg) between 0.005 and 0.05 (not #000; #0F172A ~0.008 passes); L(--surface) > L(--bg) and L(--surface-2) >= L(--surface); --ink != #FFFFFF (#EEF2F8 passes); each dark severity ink vs its soft >= 4.5:1; dark severity fill saturation <= light fill saturation for the same step; APCA Lc >= 75 for body text if computed. | secondary ([source](https://raw.githubusercontent.com/Myndex/SAPC-APCA/master/documentation/APCA_in_a_Nutshell.md)) |
| COLOR-no-pure-black-on-white | may | Avoid pure black text on pure white surfaces in the light theme; use a near-black ink on an off-white or cream ground while keeping 7:1. | Zero pure #000-on-#FFF prose pairs; contrast still >= 7:1 (prototype --ink #17233A on #F3F1EB / #FFFFFF passes both). | unverified |
| COLOR-links-not-colour-only | should | Distinguish inline links from surrounding text by underline (or another non-colour cue), not by colour alone. | 100% of inline links underlined, or link-vs-text ratio >= 3:1 plus a non-colour hover/focus cue. Prototype `a { color: var(--link) }` keeps the browser underline, so it passes unless a component removes it. | primary ([source](https://raw.githubusercontent.com/w3c/wcag/main/understanding/20/use-of-color.html)) |

### Customization and personalization

| Id | Level | Heuristic | Passes when | Evidence |
|---|---|---|---|---|
| CUST-tokens-rem | should | Declare the type scale and the target token in rem so a browser or OS font-size setting, not only page zoom, enlarges text and the targets that hold it. | Body computed font-size >= 2x baseline and .btn min-height >= 2x baseline after the html font-size change; 0 px-valued --fs-* or --target tokens. | primary ([source](https://raw.githubusercontent.com/w3c/wcag/main/understanding/20/resize-text.html)) |
| CUST-1.3.4-orientation | must | Operatory and shared tablets work in both portrait and landscape; no screen locks or nags for one orientation. | 100% of controls reachable in both orientations on both tablet profiles; 0 orientation instructions. | primary ([source](https://raw.githubusercontent.com/w3c/wcag/main/guidelines/sc/21/orientation.html)) |
| CUST-2.3.3-reduced-motion | must | Honour the OS reduced-motion setting automatically, keep the in-app Reduced motion toggle as a per-user override, and replace every removed animation with a static equivalent. | --motion resolves to 0ms and 0 animations run under OS reduce or the per-user toggle; 1 static alternative per removed animation; the toggle persists per user across reload and does not leak to other users. | primary ([source](https://raw.githubusercontent.com/w3c/wcag/main/understanding/21/animation-from-interactions.html)) |
| CUST-2.2.2-autoupdate | must | Anything that updates itself in parallel with the work (Andon, Board counts, approval badges) can be paused or has its frequency set by the user, or does not auto-update at all. | 0 auto-updating regions without a pause/frequency control; paused regions produce 0 mutations for 60 s; setting persists per user. | primary ([source](https://raw.githubusercontent.com/w3c/wcag/main/guidelines/sc/20/pause-stop-hide.html)) |
| MQ5-color-scheme-follow-os | should | Default the colour scheme to the OS preference and paint it before first render; the Light/Dark control is a per-user override with a way back to 'System'. | OS scheme honoured on first paint with 0 flash frames; override persists per user across reload; 3-state control (System/Light/Dark) present. | primary ([source](https://raw.githubusercontent.com/w3c/csswg-drafts/main/mediaqueries-5/Overview.bs)) |
| MQ5-forced-colors | must | Under forced colours (Windows High Contrast) every control keeps a visible border or outline, the focus ring uses the Highlight system colour, and no state is carried by background colour alone. | 100% of interactive controls have >= 1px visible border/outline under forced colours; 100% of focus rings visible; 0 states conveyed only by background; >= 1 forced-colors block in the shared CSS. | primary ([source](https://raw.githubusercontent.com/w3c/csswg-drafts/main/mediaqueries-5/Overview.bs)) |
| MQ5-prefers-contrast-more | should | When the OS asks for more contrast, raise body text to 7:1 and thicken lines; never treat an unqualified prefers-contrast query as a request for high contrast. | Under contrast:more every text pair >= 7:1 (large text >= 4.5:1) in light and dark; default state still >= 4.5:1; 0 unqualified prefers-contrast queries. | primary ([source](https://raw.githubusercontent.com/w3c/wcag/main/guidelines/sc/20/contrast-enhanced.html)) |
| CUST-device-over-user-density | must | A user's compact-density preference applies only on their own desk; on a shared or operatory device the device profile wins and the preference is silently not applied. | 0 sub-44 targets for a compact-preferring user on shared/operatory; preference restored on desk with 0 extra taps. | primary ([source](https://raw.githubusercontent.com/w3c/wcag/main/understanding/21/target-size-enhanced.html)) |
| CUST-2.1.4-shortcuts-optin | must | Single-character keyboard accelerators are off by default and switchable per user; only modifier shortcuts (Ctrl/Cmd+K) ship on, and every accelerator duplicates a visible control. | 0 single-key actions outside a focused component with accelerators off; 1 off switch; 100% of accelerators have a visible equivalent; state persists per user. | primary ([source](https://raw.githubusercontent.com/w3c/wcag/main/guidelines/sc/21/character-key-shortcuts.html)) |
| CUST-3.3.7-remembered-layout | should | Remember each user's layout choices (rail open/collapsed, first-shift rail hidden, last filter) under their user id and restore them on PIN switch, so nobody re-sets what they already set. | 100% of layout preferences keyed by userId; 0 cross-user leakage on shared devices; restored within the same render on switch and after reload. | primary ([source](https://raw.githubusercontent.com/w3c/wcag/main/understanding/22/redundant-entry.html)) |
| CUST-3.3.8-prefs-no-auth-cost | must | Changing a preference never triggers an extra authentication step, and the PIN that identifies the user on shared devices has at least one non-memory alternative or assist. | 0 authentication prompts caused by preference changes; >= 1 non-memory alternative or assist on the PIN pad. | primary ([source](https://raw.githubusercontent.com/w3c/wcag/main/guidelines/sc/22/accessible-authentication-minimum.html)) |
| CUST-settings-surface-small | should | Offer real users at most seven preferences, each with two to four options and a default that needs no decision; keep simulation and QA toggles (outage, after hours, device profile, grayscale) out of the production settings surface. | <= 7 user-facing preferences; <= 4 options each; 0 QA/simulation toggles visible to production personas; 1 reset control; depth <= 1. | unverified |
| CUST-os-first-tristate | should | Every in-app preference with an OS equivalent (colour scheme, motion, contrast, text size) defaults to 'System', mirrors the OS on first paint, and only stores a value when the user explicitly overrides. | 4/4 OS preferences mirrored on first paint; 0 stored values without explicit user action; override wins over OS until 'System' is chosen. | primary ([source](https://raw.githubusercontent.com/w3c/csswg-drafts/main/mediaqueries-5/Overview.bs)) |
| CUST-privacy-per-device | should | Privacy mode is a per-device setting that defaults on for operatory and shared profiles, stays on across sign-out and PIN switches, and is never a per-user preference. | Privacy on by default on 2/2 shared profiles; survives 100% of sign-outs and author switches on the same device; 0 per-user storage. | unverified |

---

## Results

_Pending: the audit and the changes follow the registration above._
