# Internal-controls module (how Precog runs inside the PMS)

> Source: generated from the approved consolidation plan (planning session of 2026-09-03; workflow `wf_8edc6cab-3ac`, 44 agents). The evidence behind every claim is under `knowledge/reviews/`. Legal statements carry one of four labels: PRIMARY, SECONDARY, REPO, UNVERIFIED (see `docs/11`).

## Controls integration (how Precog runs inside the PMS)

**Event spine.** Every repository mutation inserts a `domain_event` row in the same transaction: role granted/revoked, user deactivated, credential expired, ledger posted (kind, amount, reason, poster, effective vs posted date, hour, location), ledger reversed, allocation made, refund posted, approval requested/decided/waived, exception created/expired, deposit prepared/closed, bank transaction imported, reconciliation closed/variance/cleared, claim submitted/denied/appealed/voided/written off, procedure deleted, appointment deleted/moved, vendor created/changed, payment released, encounter signed, chart event, export performed, disclosure recorded, PHI read (volume), new device login, chain verification result, control policy changed. `pg-boss` consumers fan these to the controls engine, detectors, digest, notifications, and webhooks. The engine never reads demo data and never reads the browser; a `PracticeState` builder assembles what Precog's functions expect from live rows.

**Segregation of duties from real grants.** The current view over `user_entitlements` is the `RoleAssignment[]` input to `detectSodConflicts`. It runs synchronously on every role event and nightly, upserting `sod_findings` keyed on (rule, person set), closing findings that no longer exist, reopening recurrences, with `mitigatedSodRuleIds` from the active control policy injected so enabling a dual-release channel visibly lowers and reclassifies the linked conflicts. **Enforced:** the role-grant API refuses a grant that would create a critical conflict unless the actor records a control decision (accept residual or compensate, with a review date) in the same request; a tenant may elect the stricter "grant sits pending until a distinct second admin decides" mode. Setup seeds the role templates and shows the conflicts before the first login is issued. COSO Principles 9 (assess change) and 11 (technology controls) flip from hard-coded weak to continuously monitored because grants, terminations, MFA coverage, and shared-device posture are observed events; a terminated user still holding grants is a finding.

**Dual release enforced in the transaction.** The ledger posting service is the only writer of `ledger_entries`. For adjustment, write-off, refund, reversal, transfer, and for the vendor, ACH, check, deposit, and payroll channels, `postGuarded(channel, request)` loads the tenant's active policy and exceptions and runs `evaluateRelease` inside the transaction:

| Verdict | Action |
|---|---|
| `below_threshold`, `approved_single`, `approved_exception` | post |
| `needs_second` | insert an `approval_requests` row with the frozen evaluation, post nothing, return 202 with one line + one control |
| `approved_dual` (a distinct, role-eligible second approver decided via compare-and-set; distinct person re-checked server-side and by CHECK constraint) | the worker executes the held posting with `approval_request_id` stamped |
| `blocked_same_person`, `blocked_role`, `blocked_missing_second`, `blocked_policy_off` | roll back, return 403 with `reasons[]` and `nextSteps[]` |

A BEFORE INSERT trigger re-checks that any approval-required entry carries an approved request, so no code path can bypass the service. Exceptions (raise / lower / force / waive) require the owner role, a residual note, and an effective window; waive auto-expires; the active-exception summary surfaces expiring and standing exceptions on the owner home and in the monthly digest so a temporary raise cannot quietly become permanent. Owner overrides exist but are ledger-annotated, appear in the digest and the CPA export, and lower the Control Activities score. Denial suppression: a write-off whose claim events show a denial with no appeal routes through dual release regardless of amount, and the person who submitted the claim cannot post it.

**Hard SoD blocks at action time (a small, named set).** Full separation is arithmetically impossible in a six-person office, so `withGuard` refuses only critical pairs at runtime: whoever posted payments or prepared the deposit for a business day cannot clear that day's reconciliation variance; self-approval of vendor master changes; self-approval of payroll; requester = approver. Every other conflict is detected, scored, and requires a dated decision but is not blocked. For one-owner, one-office-manager practices the variance rule degrades to owner-only clearance rather than being disabled, and the degraded state is itself a finding.

**Independent reconciliation, measured.** Bank transactions come from an aggregator feed (Plaid or Finicity class, under data-processing-agreement and BAA review) or from OFX/CSV statement import. `reconciliation_runs.independent` is computed (the closer held no custody or recording activity that day); detection lag is measured in days between posting and matched bank transaction; match rate within 48 hours is stored. These feed the residual engine's control effectiveness as **measured operating effectiveness** alongside configured **design effectiveness**; COSO distinguishes them and so does the UI. The reconciliation logic lives in the sealed verifier package so the system policing itself is at least tamper-evident.

**Detectors (recorded, batched).** Nightly and event-driven jobs write `control_findings`: refunds above threshold lacking an approval id (should be impossible, so it is a chain-integrity alarm); postings outside location business hours; reversals or adjustments dated more than N days before posting; adjustment-to-production ratio per reason code per role against the practice baseline; void and reversal velocity; duplicate patient payments; deposit-batch vs bank gaps; unmatched bank lines older than 48 hours; sole ownership of a critical process; copy-forward documentation clusters; and, in Phase 4, the forensic suite (Benford first and second digit, round-number bias, duplicates) framed as "worth a look" control-test results. Dental's digest rules govern reporting: batched weekly, minimum sample sizes, systemic-share re-scoping, person-scoped detail readable only by the owner and a designated reviewer seat. No output is ever phrased as an accusation.

**Scoring, COSO, journal.** The typed controls registry, measured effectiveness, and derived staff composition (team size from active users, sole-owner count from single-point-of-failure detection, tenure from hire date, segregation from `segregationHealth`) feed `portfolioSummary` and `assessCoso` nightly and on control events, frozen into `control_snapshots` stamped with `SCORING_VERSION` and `CONTROL_RULEBOOK_VERSION` so a weight change never regrades history. Every finding, score, and principle carries a deep link to the rows. `tornadoSensitivity` and the cascade simulator become the settings-page "what if I turn on dual approval for write-offs" preview with dollar deltas labelled directional until calibrated. `control_decisions` is the append-only, owner-attributed control register; overdue reviews appear on the owner home; COSO Principle 17 is literally "open findings without a decision." The journal, approvals log, exception table, and hash-chained event stream together are the evidence pack for a CPA, carrier, or OCR, rendered as plain sentences.

**Where enforced vs recorded (shown in-product).** Enforced (refuse): dual release on money-moving entries; critical-conflict grants without a decision; the named runtime SoD set; requester ≠ approver; BAA-gated connector enablement; PHI egress classifier on AI and export; note-signing gates (killers, licence scope, supervision corroboration); MFA, unique identities, server idle timeout; immutability of ledger, approvals, decisions, and events at the DB role. Recorded and surfaced: SoD findings, anomaly signals, COSO and residual scores, leading indicators, decision journal, compliance tasks, PHI access review. Everything advisory says so. A channel whose data the PMS does not yet hold (payroll, until the Phase 4 payroll-provider integration) is shown as **external / attested**, never as enforced, and is excluded from scores, so the table can never show a false green.

## Feature-layer amendments (2026-09-03)

Adopted from `docs/13-innovation-and-intuitiveness.md` (the feature workflow's synthesis and its critique). Where an amendment conflicts with text above, the amendment governs.

- Exception scopes gain 'hours' (per location, per weekday, server clock) and 'vendor_state'; default policy holds refund/adjustment/write-off outside hours at any amount, excluding processor-initiated refunds. Add the walk-over approval session (60-second, single-entitlement, PIN + TOTP, disabled on operatory profiles) and the phone card contents (request id only in push; denial-no-appeal line). Add vendor quarantine as a state the evaluator reads and 'first payment after any bank-detail change is always dual'. Independent reconciliation gains a three-state grade (independent | same_hands | stale_import) and EFT matching by TRN with many-to-one tolerance falling to amber. Add the reversal-and-repost correction pair (no 'correction' kind), the late-first-posting path (posted_after_close and closed_day_id, never corrects_entry_id), and two practice-level counts, 'changed after close' and 'postings into closed days', each excluding worker-actor ERA and import rows. Decision review: measured-effect sentence from domain_event, isDateActive expiry, unreviewed decision → finding, monthly batching. Detectors: cover grant closes on observed use; 'On leave' switch enforced by withGuard; reason-code drift card links fee-schedule and plan changes in window; tip routing excludes the owner server-side (two-party reveal deferred). Enforced-vs-recorded table gains: consent-scope refusal, self-pay restriction hard block, contradiction S0 at File and Submit, for-dentist tag disposition gate, plain-language delivery gate on print/portal, prior-period refusal, vendor quarantine ('enforced on recorded releases; bank rail external'), imaging-without-interpretation on referral send. Record explicitly that no reconciliation drill runs in production; the planted skim runs on a simulated month.
- From the critique: the hours scope and the changed-after-close count apply only to human-actor postings; a CI test asserts that an overnight 835 fixture produces zero approval requests and zero changed-after-close rows.
- From the critique: the CPA seat is split. The aggregate package, the question verb, and attestations need no BAA and carry payer ids and trace numbers only; patient-level drill-down and named bank reconciliation are refused at grant time until a countersigned BAA row for the firm exists, with the refusal rendered through the Refusal component.
- From the critique: the desk PIN on Post mints or resumes the second coordinator's own server session for the single posting (the 60-second single-entitlement session designed for walk-over approvals); a PIN never annotates another person's session.
- From the critique: the S0 chart/note/claim contradiction has no reason-coded override; the only exits are the three 'Use this' corrections through each source's normal path, or a rule change under `RULESET_VERSION`.

## Implementation status (Increments 1.12 to 1.92, 2026-09-22)

What the code enforces, records, or leaves external today. The design above governs; this table says how far the build has caught up with it. See `docs/17` for the increment record.

| Design element | Status | Where |
|---|---|---|
| SoD detection from real grants (`user_entitlements` → `RoleAssignment[]`) | Built; runs on every grant and revoke and on every snapshot; nightly run not scheduled yet | `packages/controls-engine/src/grants.ts`, `apps/pms/src/lib/controls/grants.ts` |
| Grant refusal on a critical conflict without a decision | Enforced (403 inside the tenant transaction, serialized per tenant by an advisory lock, one live row per grant at the database); self-licensing refused on the grant path and the decisions route; a critical pair that an enforced, enabled channel mitigates (posting + reconciliation under the deposit dual count) is recorded and scored but not refused; strict "pending until a second admin decides" mode not built | `POST /api/controls/grants` |
| `sod_findings` with history (close, reopen, never delete) | Built | migration 0013, `apps/pms/src/lib/controls/findings.ts` |
| What a screen says when the sign-in is over | Enforced since Increment 1.81, and it was wrong before it. Seven screens read `/api/me`; four tested `!meRes.ok`, which is true of a 401 exactly as much as of a 403, and told the reader **their seat lacked the rank** — "…is for the manager and owner seats. Your seat works from the links in the header." False when the reader is the owner, and it named a header `navLinksFor(null)` had emptied for the same reason. Two more read the body and never the status, so the administrator controls vanished with nothing said. `readViewer` now draws the line where `requireAccess` draws it: **401 is the sign-in ending and nothing else**; 403 stays a fact about the account and carries the route's own words. One Refusal shape (`session-ended.tsx`, lifted out of `risk/`) says what happened and carries a link back to the screen the reader was on, through the same `sanitizeCallbackPath` guard the sign-in page uses; the header offers "Sign in" to a reader the layout cannot resolve. The state is an everyday one: `IDLE_MS` is 30 minutes at a desk and 10 in an operatory, and a stand-down, a recovery ceremony and a re-pairing each revoke sessions outright. **Also from this increment:** `withGuard` stamps every answer `Cache-Control: no-store` — nothing said a guarded answer must not be stored, and one kept on disk outlives the session that earned it, which on a shared front-desk machine is the last person's data handed to the next.  **Increment 1.82 carries the same rule to the nine screens that never read a viewer at all** — the ledger and an account, posting a payment, the approvals inbox, the day close, reconciliation and a run, statements and a statement. Each loaded straight from a guarded route with `if (!res.ok) throw new Error(body.error ?? ...)`, so an ended sign-in there showed `requireAccess`'s own sentence as a bare paragraph with nothing to press. Seventeen files held forty-two such lines; each got **one line inserted ahead of the throw** (`refuseIfSignInEnded`), so no message changed and only the 401 is lifted out. `getGuarded` and `loadFailure` hold the reading and the state change once, classified on the status and never on the sentence — `requireAccess` has four sentences for a 401 and owns all of them. The state is `sign_in_ended` throughout, matching the words the reader sees. | `apps/pms/src/lib/auth/viewer.ts`, `apps/pms/src/lib/auth/guardedFetch.ts`, `apps/pms/src/lib/auth/withGuard.ts`, `apps/pms/src/app/(app)/session-ended.tsx` |
| An act that meets a sign-in that has ended | Enforced since Increment 1.83. Increments 1.81 and 1.82 answered this on the way in; pressing a button was still the old shape, writing `err.message` into the screen's message line — so a 401 mid-press gave one sentence and nothing to press. **26 act sites across 12 screens now show the same panel a load does.** Seven of them handle a refusal without throwing, sitting in `try/finally` with no `catch`, so they set the state on `res.status === 401` rather than raising into nothing. The half-finished edit goes with the screen deliberately: the session is gone, so nothing there can succeed, and the typing could not survive the sign-in either way. **A correction to 1.82's record:** its "Not in" paragraph said acts still showed the route's sentence, which stopped being true once that increment inserted the classification at all 42 sites — the message became the error class's own, link-less and missing the one instruction the route's carried. | `apps/pms/src/app/(app)/session-ended.tsx`, the 12 screens under `apps/pms/src/app/(app)/` |
| A seat that has been opened leaves the waiting list | **Increment 1.92.** `unclaimedInvitations` asked whether *this invitation row* carried a claim, and a reissued seat has two rows (Increment 1.73). The holder can open only the one in force — `lookUpInvite` refuses a superseded link — so the claim attaches to the newer row and the older one stays unclaimed for good. The SQL filtered out the claimed row, kept the unclaimed one, and the practice read **"Invited, not yet opened" forever** about somebody who had set a password. The row's one act made it worse: `reinviteSeat` reads the invitation *in force*, the claimed one, so **"Send a new link" could only refuse** — the shape Increment 1.89 named. The question is now asked of the seat (`notExists` over the person's invitations), and the exclusion is permanent because a claim is: `reinviteSeat` refuses once the in-force invitation is claimed, so no invitation can follow one. Two tests sat one line short of this — a live case that invites once and never reissues, and a browser case that builds the exact state and stops at the claim — and both now go the extra step. | `apps/pms/src/lib/auth/invite.ts`, `apps/pms/src/lib/auth/invite.live.test.ts`, `apps/pms/src/e2e/risk.e2e.test.ts` |
| A duty the routes enforce and the rulebook carries | **Increment 1.91.** `run_import` guarded `POST /api/import/bank-statement`, `POST /api/import/curve` and `POST /api/import/curve/apply` while belonging to no catalog, so `grantEntitlement` refused it 400 `unknown_entitlement` while `revokeEntitlement` took any string it was handed — a one-way door — and the only writer was `pnpm db:seed`, which means **a practice this product set up rather than seeded could not import a bank statement at all**. `assignmentsFromGrants` also dropped it into `unknownEntitlements`, so the duty-family matrix scored nobody holding it, and Practice Risk said so out loud: *"2 grant row(s) name a duty outside the rulebook and are listed, not scored."* It is now a duty in the rulebook — **recording, weight 4**, below the weight-5 `CRITICAL_DUTIES` set so the sole-holder detector and the new-device alarm are unmoved — and the seeded owner's two combinations (recording beside reconciliation, recording beside authorization) are open and named. `CONTROL_RULEBOOK_VERSION` moves 0.2.0 → 0.3.0 and three golden hashes move with it, each traced. `check-route-guards.mjs` now fails a guard naming an entitlement the rulebook does not carry. | `packages/controls-engine/src/sod/conflict-rules.ts`, `packages/controls-engine/src/version.ts`, `apps/pms/scripts/check-route-guards.mjs` |
| Ending every sign-in in the practice | **Increment 1.90 gave it a screen.** `POST /api/admin/revoke-all-sessions` has existed since Increment 0.8 and **no screen ever called it** — the only references in the repository were the route itself and three doc comments, so an incident-response act was reachable only by somebody able to write HTTP by hand. It also hardcoded `reason: "admin_revoke_all"`, recording that the act happened and nothing about why. The route now reads a typed reason (at least ten characters, at most two hundred) and puts it on the chain beside the act, and Practice Risk carries the panel for an administrator. **The act signs out the person pressing it**, which the panel says before the press and the answer repeats afterwards; the panel reloads nothing, because every later read from that screen would meet a sign-in that is over. The typed reason is the guard in place of a confirmation dialogue. | `apps/pms/src/lib/auth/revokeAllSessions.ts`, `apps/pms/src/app/api/admin/revoke-all-sessions/route.ts`, `apps/pms/src/app/(app)/risk/sign-out-everybody-panel.tsx` |
| An alarm about the reader offers no act | **Increment 1.89, and it found a fault in 1.88.** Writing the browser case 1.88 openly left undone showed that **in the seeded practice the only holder of a critical duty is the owner** — `CRITICAL_DUTIES` is the six entitlements weighted 5, and of the seeded Ridgeview people only Riley Owner holds any. So the only `new_device_financial_role` alarm a fresh practice sees is about the person reading it, and `endSessionsForPerson` refuses that by design: ending your own sessions is a sign-out. 1.88 therefore offered a button that, for that one alarm, could only refuse. **The decision now sits where both facts are known:** the alerts route has the viewer and the event, so it clears `personId` and sets `aboutViewer`, and the board says "This names your own sign-in. To end it, sign out from the header — that needs no administrator" — the route's own guidance, arriving before the press rather than after it. `listHardEvents` stays viewer-blind, which is right: it reads rows. **The case** grants `bank_reconcile` to the front desk, plants two sessions from one unseen browser, and asserts in one pass that the owner's own alarm carries no button, that exactly one button exists, that pressing it says "Ended 2 sign-ins for Finn Front", that no live session remains, and that the chain holds one `auth.sessions_ended` naming them. Red-before measured: with the route's decoration reverted the case fails waiting for the self line. | `apps/pms/src/app/api/alerts/route.ts`, `apps/pms/src/lib/alerts/hardEvents.ts`, `apps/pms/src/app/(app)/home/owner-board.tsx`, `apps/pms/src/e2e/risk.e2e.test.ts` |
| The alarm the owner can act on | **Enforced since Increment 1.88.** The `new_device_financial_role` hard event tells the owner that a holder of a critical duty signed in from a browser nobody has seen on that account, and until now the product offered nothing to do about it. **Three things were wrong at once.** The alarm's `href` pointed at Practice Risk, which renders no hard events at all — they render on the owner's board, the digest and the CPA package — so the one link on the product's most alarming sentence led away from the alarm to a screen with no control for it. `revokeSessionsForUser` existed on the store and in both implementations, but its only caller was the enrolment route acting on the caller's own sessions: **no route let an administrator end another person's sessions**. And the one session-ending route, `revoke-all-sessions`, ends every session in the practice and has no caller anywhere — the wrong size for the alarm. `endSessionsForPerson` and `POST /api/admin/end-sessions` now carry the proportionate act at administrator rank, with the control beside the acknowledgement on the board and the event's `href` set to `null`, since there is nowhere else worth sending anybody. The event carries `personId`: its subject is the session, and ending a person's sign-ins needs the person. **The tenant check sits above the store**, because `revokeSessionsForUser` resolves the target's own tenant and would end a session in any practice if handed an id from one; a target elsewhere answers exactly as one that does not exist, so a guess confirms nothing. Ending your own sessions is refused with the way to do it instead, and a count of zero is reported plainly rather than as a failure. | `apps/pms/src/lib/auth/endSessions.ts`, `apps/pms/src/app/api/admin/end-sessions/route.ts`, `apps/pms/src/lib/alerts/hardEvents.ts`, `apps/pms/src/app/(app)/home/owner-board.tsx` |
| A username another practice already uses | **Enforced since Increment 1.87.** A sign-in name is unique across every practice (`users_username_lower_uidx`, migration 0002, on `lower(username)` with no tenant in the key), because signing in carries no practice with it — `auth_lookup_user` finds one row by username alone. `inviteAccountant`'s own check is tenant-scoped, and **row-level security is why it must be**: inside a tenant transaction it cannot see another practice's row. So a name free here and taken elsewhere passed the check, the insert violated the global index, and the error travelled out through `withGuard`, which catches nothing, to a bare 500 with no `error` field — no seat, and nothing the inviter could act on. **The insert is now the check**, taken under a savepoint so the refusal leaves the transaction usable rather than aborted; `pgErrors.ts` holds the reading, named to one index so a collision nobody anticipated is not reported as this one. **What the refusal will not say:** which practice holds the name, or anything about it. It names the rule and nothing more — saying less would restore the dead end, saying more would let one practice read another's roster a guess at a time. **A wrong first fix, caught by the reproduction:** drizzle wraps a failed statement and hangs the driver's error off `cause`, so the first reader found no `code` and re-threw; the chain is now walked. | `packages/db/src/pgErrors.ts`, `apps/pms/src/lib/auth/invite.ts` |
| A revoked duty stops opening its routes | **Enforced since Increment 1.86, and it was a control reporting success and doing nothing before it.** `revokeEntitlement` ends a grant by stamping `effective_to` rather than deleting, the way this product ends every row, and it does not end the person's sessions. Eight readers honour that convention — `staff.ts`'s `isLive`, `roster.ts`, `findingSubjects.ts`, `hardEvents.ts`, `round.ts`, `grants.ts`, `controls-engine`'s `isActive`, and the partial unique index in migration 0013. **The authorization path honoured neither half of it:** `entitlementsFor` in `postgresStore.ts` selected every row a user had ever held, and that function feeds the two reads `requireAccess` makes on every guarded request, for both the `entitlements` requirement and the `orEntitlement` opening. So the owner pressed Revoke, the screen said the duty was gone, the SoD finding closed, the `role.revoked` event was written — and the person kept the duty on the session they were sitting in and on every session after it. `liveGrants.ts` now holds the rule once (a grant has begun and has not yet ended; the comparison against the end is strict, because a revoke stamps the instant it happens), and `staff.ts` borrows it rather than restating it — restating it is how the auth path came to state it nowhere. `now` is the caller's clock, matching the revoke; a grant written with the database's clock on a skewed deployment reads as not yet begun for the length of the skew, which fails closed. **A correction to the finding that produced this:** it also claimed a future-dated grant opens early. The missing `effective_from` predicate was real; no product path reached it, since every writer stamps `now`. That half is a hazard the predicate closes, recorded as such rather than counted as a second defect. **Why nothing caught it:** the revoke test asserted the finding closes and that a second revoke answers 404, and never asked what the person could then do; the store test asserted a user's entitlements equal the three the seed granted, and no seeded row has ever been revoked. Both now ask, proved red-before and green-after. | `packages/db/src/liveGrants.ts`, `apps/pms/src/lib/auth/postgresStore.ts`, `apps/pms/src/lib/controls/staff.ts` |
| The seed's story week, and the day it expires | **Increment 1.84, and a stopgap the owner chose knowingly.** The seed writes `effective_date` as a literal while `posted_at` is the moment it runs, so the gap belongs to the wall clock. `BACKDATE_DAYS = 7` raises a `retroactive_entry` past seven days, and on 2026-09-22 the 2026-09-14 week went eight days stale and turned `main` red for nobody's change — the rule was right, the fixture was not. Offered three ways out, the owner chose to move the written dates forward rather than anchor them to the seed run. The week is now 2026-09-19 (and 2026-09-21 for the statements), five days rather than six because the money-desk suite imports deposits at `daysAgo(2)`, which on the 22nd is the 20th. **Any written date buys at most seven days**, since `today - effective` must stay at or under seven and may not run ahead of the clock. **This one fails again on 2026-09-27.** **Increment 1.85 gave that sentence a reader.** The week is now one constant — `SEED_STORY_WEEK` in `seed-data.ts`, carrying `effective` and `issued` — which the four seed modules, the three demonstration defaults and the seven money-desk assertions read rather than repeat, so moving it is one edit where it was eight; the arithmetic note moved onto the constant, and `seed-ledger.ts` keeps a pointer. `seedWindow.ts` holds the three rules the week must satisfy and `seedWindow.test.ts` runs them against the real clock, so on the day of expiry the gate names the constant, the arithmetic and the two ways forward before the owner-board cases reach their assertions. **The third rule cost a run to find:** a week may not run ahead of the clock, may not sit more than `BACKDATE_DAYS` back, and may not sit exactly `DEPOSIT_DAYS_AGO` back — the offset the money-desk suite banks its deposits at, which puts four rows on a day close that expects two. Whoever next picks a date will reach for the maximum the arithmetic allows, and that collision lands two days later on a day nothing else explains. The guard does not stop the board cases going red; it fails beside them, with the sentence. | `packages/db/src/seed-data.ts`, `apps/pms/src/lib/controls/seedWindow.ts`, `apps/pms/src/lib/demo/dates.ts`, `packages/db/src/seed-ledger.ts`, `seed-day-close.ts`, `seed-statements.ts`, `seed-approvals.ts` |
| Second factor, and getting back in | Enforced since Increment 0.4 (the middleware holds an unenrolled session at `/enroll-mfa` and nowhere else) and, since Increment 1.76, no longer one-way: `mfa_enrolled_at` was written once and cleared nowhere, both enrolment functions refused an enrolled account, the screen bounced anybody enrolled to `/home`, and recovery codes only ever decreased — ten sign-ins on codes plus a lost phone left an account nobody could reach, the owner's included. A signed-in person may now pair a new authenticator and receives a fresh set of ten codes in the same act; a pairing in progress lives in `mfa_pending_secret_enc` (migration 0054) so the working factor survives an abandoned attempt, and neither auth lookup returns that column. Increment 1.77 reaches the person who cannot sign in at all. The two-administrator recovery ceremony, which had three working routes and no surface since Increment 0.6, now has screens: Practice Risk carries "Getting somebody back in" and `/regain/[ref]` is the third page outside a session. A consumed ceremony sets the password **and** clears the second factor (`clearMfaEnrollment`, one write), so the next sign-in lands on the enrolment screen; `auth.recovery.consumed` carries `secondFactorCleared`. Migration 0055 fixes a grant that had made every approval and consume fail against a real database since 0007. The target may no longer approve their own recovery — refusing only the initiator left the attacker and the victim as the pair. **No governed exception, deliberately:** one administrator who can clear a factor and set a password can post and approve as that person, which is what maker-checker prevents, so a practice with one administrator is refused before a ceremony exists and told to appoint a second. The second administrator that refusal asks for became appointable in Increment 1.78; see the row below. Increment 1.80 closes the last trap in this cluster: **finishing an enrolment revokes the session that reached it, and the browser's signed cookie used to survive that**, still carrying the `needsMfaEnrollment` claim, which the `jwt` callback writes at sign-in and never again. The middleware believed it — on the edge there is no row to read — and redirected every path back to `/enroll-mfa`, `/signin` included, where the screen's own route then answered 401 because that session was gone: one error line, a disabled button, and the only exit sitting in React state a reload discards. The screen now reads the route's `signOut` field, present since Increment 1.72 and until now unread, and ends the cookie in the same breath as the row while the codes stay on screen; it reads a 401 as a session that is gone rather than as an error beside the field, and says so with the way out; and `enrollmentGateAllows` admits the sign-in page, because a gate may hold somebody out of the application and may not hold the door. | migrations 0054 and 0055, `apps/pms/src/lib/auth/enrollMfa.ts`, `apps/pms/src/lib/auth/recoveryCeremony.ts`, `apps/pms/src/lib/auth/regainAccess.ts`, `apps/pms/src/lib/auth/enrollmentGate.ts`, `apps/pms/src/middleware.ts` |
| Who is on the practice | Nothing wrote `users.role` before Increment 1.78: no route set it, the only `insert(users)` hard-codes `readonly` for the accountant's seat, and the auth store updated `active`, the second-factor columns and the password alone. That was the root cause behind Increments 1.75 and 1.77, and it made 1.77's refusal name a remedy the product could not obey. `changeRank` now writes it, refusing an unknown rank, somebody outside the practice, a deactivated account, a change that moves nothing, an administrator changing their own rank, and lowering the last administrator. **No control decision is asked for, and that is a finding rather than an omission:** the SoD rulebook scores duty combinations from live grants and infers nothing from a role label, so a rank creates no conflict and a gate modelled on the grant path would refuse nothing ever. What a rank moves is *signing power* — `listEligibleApprovers` reads the release rules against the label — so `signingShift` reports the channels each change opens and closes, on the screen and on the chain (`role.rank_changed`). One administrator may appoint another, because requiring two to make a third is circular; the containment is attribution rather than prevention. Increment 1.79 adds the other half of the roster. `users.active` had been in the schema since Increment 0.2 and never reachable: `deactivateUser` had two callers, both tests, and nothing set the column back. `setPersonActive` now takes a direction, refusing somebody outside the practice, a state that is already the state, standing yourself down, and standing down the only administrator. **Deactivating ends every live grant and revokes the sessions in the same transaction** — the grants because `detectSodConflicts` never reads `active`, so the column alone would leave a departed colleague's duty conflicts standing forever, and ending live rows clears them through the mechanism already there rather than redefining what the rulebook scores; the sessions because `authorize` refuses an inactive account on the next sign-in and a session already minted is not a sign-in. **Reactivating restores the account and not the powers**, so each grant is decided again through `evaluateGrant`; the password and second factor are untouched by either direction. The last-administrator refusal is no more reachable here than for a rank change — the guard makes the actor an active administrator and the self rule makes the target somebody else — and is kept as the net under both. **Still open:** inviting a new person at a rank, which waits on the business-associate question this document leaves with the owner. | `apps/pms/src/lib/controls/ranks.ts`, `apps/pms/src/lib/controls/roster.ts`, `packages/controls-engine/src/signing.ts`, `apps/pms/src/app/api/controls/ranks/route.ts` |
| Chart-of-accounts maker-checker, and the practice that cannot staff it | Enforced at the database since migration 0028 (a mapping's decider is never its proposer) and, since Increment 1.75, enforced as "never without a recorded decision": migration 0053 replaces the CHECK with a trigger reading the append-only decision register, because a practice whose only rank-bearing person is one administrator could otherwise propose a mapping nobody may decide, leave every journal line unmapped, and close no month ever — the product writes `users.role` nowhere, so it cannot appoint a second decider. The way out is an `accept_residual` or `compensate` decision on the control subject `gl_mapping_maker_checker`, carrying a reason and a required review date; the licence is derived from the newest decision on that subject, so retiring it tightens the control back with nothing to correct, and an overdue review still licenses while being reported as overdue (Increment 1.31's reading). The decision is recorded by the person it licenses, deliberately — requiring a second administrator would reproduce the deadlock one level up — and what stands in for the second pair of hands is that every month-end package counts the mappings its own figures were read through that one person both proposed and decided, inside the package hash at `package-v7`. | migration 0053, `apps/pms/src/lib/cpa/soleDecider.ts`, `apps/pms/src/lib/cpa/mappings.ts` |
| Dual release inside the posting transaction | Enforced for write-off and adjustment; partial for check and ACH (patient refunds and transfers only, no vendor payments held, so no SoD credit). The BEFORE INSERT trigger re-checks every guarded insert against the active policy: an approved request decided by a different person for the same channel and amount, or an in-window exception, or the row is refused. A second person named inline is never a decision. | `packages/ledger/src/postGuarded.ts`, migration 0014, `apps/pms/src/lib/controls/decideAndPost.ts` |
| Deposit channel (dual count before the bag is sealed) | Enforced at the day-close freeze: the freezer is the second counter and must be a different, role-eligible person than whoever prepared the day's deposits; the preparer and an ineligible role are refused (403) with the names of who could count instead; a one-person office degrades to owner-only sealing recorded as a finding on `day_closes.summary`; the verdict is frozen into the close and onto the chain (`day_close.frozen`). The channel therefore counts toward dual control in the scores. | `POST /api/day-close/freeze`, `apps/pms/src/lib/day-close/seal.ts` |
| New vendor, payroll channels | External / attested, and since Increment 1.51 the word has something behind it: a dated assertion per practice, month and channel (`channel_attestations`, migration 0038, append-only under FORCE RLS, one row per month per channel and never rewritten) that a named person reviewed that channel's month, made from the Attest section of `/cpa` and read back on the Practice Risk coverage row for the month that has ended — where nobody has spoken, the row says so rather than letting "attested" imply somebody had. Only a channel this build cannot enforce may be attested: attesting an enforced one is refused, because an attestation beside evidence the product already holds adds nothing and reads as though it did, and the attestable list is derived from `ENFORCEMENT` so moving a channel up removes it in the same change. The seat is recorded beside the name, because an attestation the practice makes about its own external channel is worth less than an independent reader's and a reader of the table is entitled to tell them apart. The month-end package carries the month's attestations, which moved the package schema to `package-v4`. Since Increment 1.52 the package also states the other half — what nobody vouched for — as a tie-out row of its own, "Every channel the product cannot enforce was reviewed by somebody" (`package-v5`), and the owner board carries the same reading as a card for the month that has ended. Both read one pure function (`attestationCoverage`) over the same rows, so the tie-out and the board say it in the same words rather than in two that could drift; a live case asserts that equality at each of three states (nobody, one of two, both). The card is on the board in every state, the covered one included, because an owner who sees it only when it is red cannot tell a reviewed month from a month nobody looked at. A practice whose build enforces every channel reads complete rather than "0 of 0", since a false red of that shape is the failure this row exists to prevent. The month read is the one that has ended, never the one still filling: a month nobody could have finished reviewing is not a month anybody reviewed, and one `lastCompleteMonth` serves every surface that says so. Since Increment 1.53 the weekly digest is the fourth surface, and the one that had to be split in two to stay honest: it counts the channels somebody attested inside its seven days, which is a fact about the week and so rides inside the figures the owner stamps as read and inside the month-end hash (`package-v6`), while what the practice still owes for the month that has ended rides beside the digest on the route and never inside it. A standing figure in the digest would move the hash of every closed month the moment anybody attested anything, and would make an acknowledgment already given read as stale — the false positive Increment 1.36 removed, which the same coupling would have reintroduced in a new place. The page says which is which in those words, and a unit case asserts the split by the shape of the object rather than by convention. An attestation is counted in the month the act happened and reported by the tie-out of the month it speaks for: those are two different months whenever somebody vouches for a month after it ends, which is the only time anybody can. External / attested: the signed-in user attests a release, no second signer is taken from the request, the verdict is never a completed dual release, and the record is excluded from scores | `POST /api/controls/release/evaluate` |
| Exceptions (raise / lower / force / waive) | Built as policy versions under a per-tenant lock; owner rank, typed fields, reason, residual note, window; waiver ≤ 90 days; 'hours' and 'vendor_state' scopes not built | `POST /api/controls/exceptions`, `/retire` |
| Decision register (`control_decisions`) | Built, append-only; a control-wide decision governs every conflict of the control, a finding's own decision wins; overdue reviews surfaced; `measured_effect` and monthly batching not built | `POST /api/controls/decisions` |
| Residual, COSO, leading indicators, tornado, `control_snapshots` | Built; frozen with both version stamps; nightly job freezes one snapshot per tenant and refreshes findings (`pnpm controls:snapshot`, `.github/workflows/nightly-controls.yml`) | `POST /api/controls/risk`, `apps/pms/src/lib/controls/nightly.ts` |
| Enforced-versus-recorded table and the Practice Risk surface, shown in-product | Built at `/risk` (manager rank to read, administrator rank to act): provenance sentence (frozen or live, both version stamps, "directional"), four headline tiles, the six-channel enforced / partial / attested table, duty combinations with the governing decision state, who holds which duties with grant and revoke, the Refusal component on a refused grant with "record the decision and grant in the same step" only for an unmitigated critical pair, the decision register with overdue marks, standing exceptions, and the assumptions list. CI drives the page in Chromium against the production server on a throwaway database (`pnpm e2e`). The CPA month-end package is built as its first slice (Increment 1.34; `/cpa`, manager rank to read, administrator rank to export): one calendar month computed from rows on every read, aggregate and PHI-minimal (journal by ledger bucket and kind, adjustments and write-offs by reason code with approval counts, the deposit register, the digest's counts over the month, coverage and standing exceptions and the decision register at month end, attestations on external channels, the chain head and last check, a three-line tie-out), hash-stamped, exported as CSV or JSON with each export a `cpa.package_exported` chain event carrying the hash and row count; since Increment 1.35 each journal line carries the account the practice mapped it to, or reads unmapped: `gl_mappings` (migration 0028) holds one row per proposal, keyed by ledger bucket, posting kind, and reason code, proposed by one person and approved by a different one (the database enforces maker-checker with a check constraint, permits one pending proposal per line, and refuses every edit but that one decision), with both the proposal and the decision on the chain; since Increment 1.36 a month that has ended is closed by the owner (`month_closes`, migration 0029: one append-only row per month freezing the package hash, the entry count, the journal total, and who closed it; a closed month is never re-opened, and closing is refused while any journal line has no approved mapping), and a database trigger refuses any entry effective-dated into a closed month unless it carries reason `prior_period`, so a correction posts today, is reported in the month it posts, and raises the retroactive-entry hard event whenever the gap exceeds seven days. Since Increment 1.37 that refusal needs the pair and not only the label: a correction is a reversal mirroring the entry it clears and a repost carrying the corrected figure, written in one transaction, both naming the entry they correct (`ledger_entries.corrects_entry_id`, migration 0030), with the database refusing a reversal of a reversal, a second reversal of one entry, an unmirrored amount, and a repost with no reversal behind it; and the account ledger offers Correct on a row that is neither half of a correction nor a charge. Since Increment 1.38 a correction whose figure needs a second person waits as one approval request naming the entry it corrects (`approval_requests.corrects_entry_id`, migration 0031): nothing posts until the decision, the approval then writes both halves in one transaction under that one request, and the database admits neither half above the figure the second person approved, one row per kind and no more. Since Increment 1.39 the approvals inbox reads a held correction as a correction — the entry it replaces, the figure now, the figure proposed, and that approving writes both rows while declining writes neither — and the reason a correction carries is chosen from the codes the practice has adopted: `ledger_entries.reason_code` is a foreign key into `reason_codes`, so the service refuses an unregistered reason in words rather than failing the insert, and the seed carries `prior_period` so a practice can correct a closed month at all. The package hash covers the figures the package states about the month and none of those that state the practice's position right now (the chain head, the last chain check, open findings, overdue reviews, the chart-wide mapping counts), corrected in Increment 1.36 so that a closed month is reported as changed only when a figure about the month moves — re-mapping a journal line, or editing the policy the package reports as in force at month end; since Increment 1.49 the CPA seat itself is built: an outside accountant signs in at the lowest rank the product has, holding `view_reports_only` and nothing else, and reaches `/cpa` because `withGuard` gained `orEntitlement` — a grant that opens a route in the rank's place, relaxing the rank alone while a declared `entitlements` list still has to be held in full. The seat reads the package and exports it (each export a chain event naming who did it) and is offered neither the chart of accounts, which is the practice's own maker-checker, nor Close month, which is the owner's irreversible act; every other route refuses it exactly as before. The seat needs no BAA because the package names no patient, which a live case now proves by reading the practice's own `patients` and `guarantor_accounts` rows and looking for any of their names, MRNs, or ids in the package and its CSV — the condition docs/07 question 25 sets for letting an outside firm read anything; patient-level drill-down and named bank reconciliation stay refused until a countersigned BAA row exists for the firm. `view_reports_only` pairs with no duty in the conflict rulebook, so inviting the seat creates no finding, and the seat carries its own control-role label rather than falling through to "Front Desk Lead", a role the write-off channel names as a first approver. The header now offers each viewer only the screens its rank and grants open, from one catalog whose declared gate a unit test reads back against each route file; since Increment 1.50 the package is no longer one-way: either side opens a thread on a line the package states (`cpa_thread_messages`, migration 0037, append-only under FORCE RLS), the accountant from `/cpa` and the practice from the owner board, with both halves on the chain (`cpa.question_asked`, `cpa.question_answered`). The line is chosen from the ones the package reports rather than typed, and a question about a line the month does not state is refused; the seat a message is recorded under is read from the signed-in user rather than the request body, because which side spoke is what decides whether the practice still owes an answer; and nothing is edited or deleted — an answer that was wrong is followed by another message, the treatment a posting gets from a correction. Who owes the next word is read from the last message's seat, never from a status column that could disagree with the messages under it; the board resolves no line labels, because one label costs a whole month-end package to compute and the question's own words carry the meaning. Since Increment 1.54 a thread about a month the practice has since closed says so, on the `/cpa` thread and on the owner board alike, from one pure sentence naming who closed the month, the day, and what a fix now does: the figures are frozen, the accountant already holds them, and a correction to that line posts today under `prior_period` and is reported in the month it posts. Read alike, a thread about a closed month and one about an open month are indistinguishable, and they call for different answers — "I will fix that figure" is true of the second and false of the first. The board resolves the close although it deliberately resolves no labels, because a close is one indexed read for every thread rather than a package apiece, and because the owner needs it before they answer rather than after. The note is the thread's own month and never the practice's latest close, and the sentence cites the reason code the closed-month refusal is enforced from rather than spelling it a second time. Since Increment 1.55 the thread is no longer lopsided: the practice learned it owed an answer from the first, because the board reads the last message's seat, while the accountant learned nothing and had to re-read every thread of every month to find one. A badge for "the practice has answered" would never clear, and a signal that is always on is not a signal, so what clears it is a record that somebody read the answer — stamped like the digest acknowledgment of Increment 1.28 rather than written by the act of loading a screen, because a GET that writes makes the record a side effect of a page view and that is a weaker claim than a month-end file needs. `cpa_thread_reads` (migration 0039, append-only under FORCE RLS) holds one row per read act carrying the message the reader had in front of them, and is deliberately **not** unique per thread and seat: a thread is read again whenever it grows, the latest row wins, and a later message re-opens the signal without any row being rewritten. The database refuses a read naming a message of another thread, because a read pointing elsewhere would clear a signal about a conversation the reader never saw; the service refuses a thread whose last word is the reader's own (a message you wrote is one you have seen) and a second mark while nothing has been said since, so the table carries acts rather than a pile of rows asserting the same thing. Each seat is read separately, so one side marking a thread read never clears the other's, and each read is a `cpa.answer_read` chain event. since Increment 1.51 the attestation tab is built too (previous row); the digest is (next row) Since Increment 1.48 each channel row also names the active reason codes carrying a threshold of their own, loosest first, under the channel figure they tighten: a reason may hold a channel to less than the channel's own figure (Increment 1.46), so a reader of the coverage table who could not see them was reading a figure that no longer governed every posting on that channel. Where a reason's figure is at or above the channel's it holds nothing extra, and the row says so in words rather than printing a figure that looks like a tightening. Where the practice loosened a reason's threshold, the row names the decision that licensed it, who decided, and the review date; tightening back retires that decision (Increment 1.47) and so takes the naming off the row by itself. The row names the reason and changes nothing — `/reason-codes` is where a threshold moves. Since Increment 1.74 the delivery panel — where a person says where their notices go, proves the address reaches them, and reads the message that would be sent — is no longer this screen's alone: the outside accountant's seat holds the same three routes by entitlement (`orEntitlement: CPA_SEAT_ENTITLEMENT`) and cannot open this screen, so the panel and its four acts are shared components rendered here and on `/cpa`, and the message carrying a code names the screen its reader can open, read back out of the same navigation catalog the header draws from rather than named a second time. | `apps/pms/src/app/(app)/delivery-panel.tsx`, `apps/pms/src/app/(app)/delivery-acts.ts`, `apps/pms/src/app/(app)/risk/`, `apps/pms/src/e2e/risk.e2e.test.ts` |
| Owner home (`docs/04` Owner row) | Built at `/home` for manager rank and above, from live rows on every read: "Yesterday reconciled?" as one shape and word with one action (no bank record, N variances, yesterday not closed, or the three-state independence grade), the 48-hour match rate and median detection lag, approvals only the viewer can give, decisions due for review within 30 days (overdue first), exceptions expiring within 14 days, and practice health with the tornado's top three levers, labelled directional. Copy describes hands, never a person. Each decision due carries one practice-wide sentence on what happened since it was made (postings, guarded releases with and without a second approver, bank runs cleared and how many owner-only, detector findings opened and closed; directional, no one named), and an administrator reviews it there: Keep (the same decision, 90 more days), Tighten (Remediate, 30 days, with a note), or Retire (a `retire` row, migration 0023, after which the subject reads as undecided again). Each review is one superseding row at `POST /api/controls/decisions/review`; nothing renews on its own. Since Increment 1.52 the board also carries the channels this build cannot enforce for the month that has ended: how many of them nobody has reviewed, the sentence naming which, and a link to the attestation tab while any is missing. Where every one carries an attestation the card names who said each, marking the practice's own word as the practice's, because an attestation a practice makes about its own external channel is worth less than an outside reader's. Tender rows expected against bank and the changed-after-close line are not built | `apps/pms/src/lib/home/board.ts`, `apps/pms/src/lib/home/measuredEffect.ts`, `GET /api/home/board`, `POST /api/controls/decisions/review` |
| Weekly digest with acknowledgment stamping | Built at `/digest` for manager rank and above (Increment 1.28): seven days ending on a chosen date, computed from rows on every read: ledger postings by kind with totals, guarded releases with and without a second approver, approvals requested, given, declined, and cancelled, statements imported, bank runs cleared and how many owner-only, variances cleared with a reason, deposits prepared, day closes frozen, patient statements issued, held, and voided, detector findings opened and closed by kind and open at week's end, decisions recorded by kind, reviews kept, tightened, and retired, decisions past review at week's end, snapshots frozen, sign-ins, MFA enrolments, session revocations, duties granted and revoked, policy changes, and the chain's event count and sequence span, with any other event kind listed raw. The query layer carries no person dimension, so no row names anyone; rates are not shown because a week is too small a sample (`docs/05` minimum-sample rule). An administrator stamps a week as read: one append-only `digest_acks` row (migration 0024, RLS forced, never updated or deleted) binding a sha256 of the canonical digest they read and the chain's event count, plus a `digest.acknowledged` chain event; a second stamp is refused, a stale hash is refused, and the page says when the rows changed after the stamp. Batched, never alerting; the six hard events have their own card (next row); nothing pushes | `apps/pms/src/lib/digest/digest.ts`, `GET /api/digest`, `POST /api/digest/ack`, `apps/pms/src/app/(app)/digest/` |
| Hard events (immediate owner alerts) | Built (Increment 1.29) as a read model over rows, computed on every read of the owner board, administrator rank only, last seven days: an after-hours refund (a refund posted outside the location's stored business hours in the location's timezone, from the server clock; `locations.hours`, migration 0025, default 7:00 to 19:00 Monday to Thursday, 7:00 to 17:00 Friday, closed weekends, editable by the owner at `/locations` since Increment 1.32, every change a `location.hours_changed` chain event; since Increment 1.30 the sentence says whether the hold caught it and a second person approved, whether it is still held and waiting, or whether the row exists with no approval at all, which the trigger should have refused); a retroactive-dated entry (any posting more than seven days after its effective date); a waived dual control (a `control.policy_changed` chain event adding a `waive_dual` exception); a deposit variance over threshold (a bank run whose variance against the practice's deposits exceeds $100, a constant until the policy holds a variance threshold); an audit-chain check failure (an `audit_chain_checks` row with ok false); a new device on a financial role (the first session from a browser signature an account holding a weight-5 duty has not presented before). Each event names a row, a location, a time, or a duty, never a person, and links to its page. Since Increment 1.33 the owner acknowledges each event with a note of what was done (`hard_event_acks`, migration 0027, append-only, one per event, a `hard_event.acknowledged` chain event beside it); the card reads "Seen by … on …: note" and says how many still wait, and the digest counts hard events acknowledged and after-hours holds. No push channel exists; the card on the owner board is where they land, and everything else waits for the digest | `apps/pms/src/lib/alerts/hardEvents.ts`, `GET /api/alerts`, `apps/pms/src/app/(app)/home/owner-board.tsx` |
| Independent reconciliation, measured | Built as a three-state grade over the last 45 days of cleared runs from a bank source: `independent` when every cleared run in the window was cleared by someone who neither prepared a deposit nor posted a payment in its period (the same test variance clearance applies), `same_hands` when any was not (owner-only clearance recorded as a finding counts as same hands), `stale_import` when nothing was cleared in the window. The grade feeds `independentBankRec` (COSO monitoring, the cash controls' residual, the leading indicator) and is frozen into the snapshot under `measurements.reconciliation`; the assumptions list states the grade and its reason instead of "not measured". The grade is shown on the reconciliation screen and on Practice Risk | `apps/pms/src/lib/controls/reconciliationMeasure.ts` |
| Detection lag and the 48-hour match rate | Measured from bank lines posted in the last 45 days that are at least 48 hours old: the median days from bank posting to the practice resolving the line (matched at import, cleared or waived at clearance, open lines at their age today as a lower bound), and the share of bank credits matched to a practice deposit within two calendar days. Bank credits match the deposits the practice prepared in the product and the Curve Hero deposit-slip rows it staged, each once across every run; debits are matched by nothing yet and count toward lag only. Frozen into the snapshot under `measurements.matching` and shown on the reconciliation screen and on Practice Risk. Recorded, not scored: no weight reads them until a CPA calibrates one, and the assumptions list says so. The aggregator feed and EFT matching by trace number are not built | `apps/pms/src/lib/controls/matchingMeasure.ts`, `apps/pms/src/lib/bank/import.ts` |
| Knowledge map and sole-owner detection | Not built; counts as zero and named as an assumption | — |
| Detectors and `control_findings` | `control_findings` built (migration 0019): one row per (kind, subject), open while the condition holds, closed with a reason when it clears, reopened on recurrence, never deleted; RLS forced; the runtime role may select, insert, and update, never delete. Eight detectors run: unmatched bank lines older than 48 hours (judged on the earliest run that carried the line, graded low under a week, medium to a month, high beyond, closed when the line matches or its run clears); owner-only clearance (a run cleared with no other eligible person inside the 45-day window, medium, closed when the run leaves the window); a decision past its review date (an active decision whose review date has passed, medium to a month over, high beyond, closed when a new decision supersedes it); a release above threshold without approval (a guarded-channel entry above the active policy's threshold citing neither an approved request nor a policy exception, high, the chain-integrity alarm, judged over every row against the active policy); a posting dated well before it was posted (a reversal, adjustment, or write-off posted more than seven days after its effective date, medium to a month, high beyond, inside the 45-day window); a duplicate patient payment (two or more on one account with the same amount and effective date, none reversed, low on the later rows, inside the window); a deposit not yet at the bank (a prepared deposit at least five days old inside the window that no imported bank credit has matched, medium to two weeks, high beyond, closed when a credit matches it); a critical duty held by one person (a duty of risk weight 5 held live by exactly one active person, medium, closed when a second holder is live or none is). Every finding is worded about a line, a run, a decision, an entry, a deposit, or a duty and never a person. Detectors run on every frozen snapshot (nightly job and manual freeze); findings are read at `GET /api/controls/findings` and shown on Practice Risk; the owner board counts the open ones and, of those, the ones without a decision. An administrator records a control decision on an open finding (`subjectKind: "detector_finding"`, migration 0022): any decision kind, superseding as for any other, reviewed by the unreviewed-decision detector; the detector still owns the row's status. Whoever the row is about (the one holder of a sole-held duty, who cleared the degraded run, who made the overdue decision, who posted the entry, who prepared the deposit) may not accept its residual or name a compensating control for it. The other detectors docs/05 lists (out-of-hours postings, which need a business-hours model; adjustment-to-production drift by reason code; void and reversal velocity; the forensic suite) and the weekly digest are not built | migrations 0019 to 0022, `apps/pms/src/lib/controls/detectors.ts`, `apps/pms/src/lib/controls/findingSubjects.ts` |
| Hard runtime SoD blocks | Variance clearance: enforced (Increment 1.11; whoever prepared deposits or posted payments in the period cannot clear, owner-only clearance recorded as a finding). Deposit seal: enforced (Increment 1.14). After-hours hold: enforced (Increment 1.30; a refund, adjustment, or write-off posted outside the location's stored business hours, read from the server clock in the location's timezone, is held for a second person whatever the amount, by an hours-scoped `force_dual` exception every tenant starts with; the database trigger refuses such a row without an approved request, so the hold cannot be bypassed by any code path; the held card and the inbox say the clock and the window; since Increment 1.31 switching the hold off is a governed change: the owner records an accept-residual or compensate decision with a required review date on the Practice Risk exceptions table, the decision and the new policy version are written together, the owner home reads "After-hours hold: off since, review due" until it is switched back on, and switching it on retires that decision; the same rule holds for any exception that tightens a control, while retiring a raise or a waiver needs only a reason). Vendor self-approval and payroll self-approval: not built; the product holds neither entity. Since Increment 1.40 a sealed day also reports what arrived behind it: a database trigger stamps every ledger row it admits against a frozen day close for that location with `posted_after_close` and `closed_day_id` (migration 0032), reading the seal at insert time rather than deriving it from dates, overwriting whatever the writer passed, and on a column the table grants no UPDATE on, so the stamp is the database’s own observation and no later hand moves it. The row is admitted rather than refused — a payment that arrived arrived, and the month close is where the refusal belongs — and the day-close page carries the lock beside the word "Frozen" and a "Since the seal" section naming each late row, whether it is a first posting or half of a correction, who wrote it, its reason, and what the rows together move the day by, with the sealed figures left exactly as the practice counted them. Since Increment 1.41 the owner board reads those stamps back as two practice-level counts on a "Sealed days" card: what landed behind yesterday’s seals, and what landed behind any seal in the last 30 days, each with the rows, the net they move the sealed days by, how many sealed days they touched, and how many are first postings rather than halves of a correction. The card appears in every state, the quiet one included, because an owner who never sees it cannot tell a clean practice from a broken count; the window is the question a single clean day cannot answer, since sealing a day at the figure the owner expects and posting the rest later shows up as a habit long before it shows up as a variance. Nothing on the card names a person. Since Increment 1.42 the same stamps drive a ninth detector and a seventh hard event: `posting_into_sealed_day` (migration 0033 widens the finding kinds) opens one `control_findings` row per first posting behind a seal inside the 45-day window, medium on its own and high once a second lands behind the same seal, closing when the row leaves the window; and the owner board pages on each one at a time rather than waiting for the digest. Halves of a correction are excluded from both, because a correction names the entry it replaces, carries a reason, and above the threshold waits for a second person, while a first posting into a sealed day announces nothing. Since Increment 1.43 the weekly digest counts them too — postings into sealed days over the week, and how many are first postings — which was blocked until the package carried a schema version, because `hashedView` folds the whole digest into the month-end hash: `PACKAGE_SCHEMA_VERSION` now sits inside the hash and `month_closes.package_schema` records it at close (migration 0034, backfilling months closed earlier to `package-v1` and then dropping the default), so where the shape has changed the CPA page says the two hashes do not compare rather than reporting a figure moved, and falls back to the entry count and journal total the close froze in their own columns, which no change of shape touches. Since Increment 1.56 that honesty no longer costs the answer outright. A month closed under an older shape may be given a **baseline** (`month_close_rehashes`, migration 0040, append-only under FORCE RLS): what the month hashes to under the shape in force now, taken once per shape by an administrator, so "has a figure moved?" is answerable again — from the baseline's date rather than from the close's, which the sentence says in those words. The close's own frozen hash is never recomputed onto the row: it records what the accountant received, `month_closes` refuses every update and delete, and overwriting it would assert something false about the past to answer a question about the present. The database refuses a baseline under the shape the close itself froze (that close is already its own baseline), a second baseline under one shape (the first reading is the baseline; a second would move the line a later comparison is drawn from and hide a move in between), and one for a month never closed (a foreign key into `month_closes`, since there is nothing frozen to compare against). Each baseline is a `month.rehash_baseline` chain event, and taking one is the practice's act rather than a background job, because it changes what a later reader may conclude and from when. Since Increment 1.57 the Practice Risk page carries a **What people owe** section folding those readings into one list: the channels nobody vouched for, the questions waiting on the practice, the answers waiting to be read by the accountant, and the decisions past their review date. It is derived on every read and stored nowhere, because a notices table would be a status column that can disagree with the rows under it — the thing this product refuses from the owner board's counts to a thread's `awaitingPractice`; an entry therefore cannot outlive the fact it reports, which a live case proves by driving one thread from asked to answered to read and watching the entry move seat and then go. Each entry carries the sentence its own surface already writes rather than a second wording for one fact, and names the seat that owes the doing, since the owner cannot discharge the accountant's reading and the accountant cannot answer the practice's question. An empty list is a real state and says so in words. Since Increment 1.58 that list can be rendered into the message that **would** be sent, and the product records where it would go; nothing is sent. The rule the increment exists for is what a message may contain: two of the four notice kinds carry a person's typed words verbatim (Increment 1.50 chose the question's own words over a resolved label), nothing constrains what somebody types into a thread, and a message lands outside every guard the product has — in an inbox, on a lock screen, in a forward — so a message that quoted one could carry a patient's name out of a product whose outside-accountant seat needs no BAA precisely because what it reaches names none. Each notice therefore carries an `outside` sentence as well as the screen's: generated here from months, dates and codes, saying that the accountant asked and when rather than what they asked. The rule is held by the type rather than by care — `renderMessage` takes `Omit<Notice, "sentence">`, so nothing in that module can read the typed words even by mistake, and a unit case drives a thread whose body is a distinctive string and asserts the rendered message does not contain it. The subject carries the practice and a count and nothing else, because a subject is the part a person sees without choosing to look; and a seat that owes nothing gets no message at all, since one that arrives whether or not anything happened is not a signal. Where a message would go lives in `notice_addresses` (migration 0041, append-only under FORCE RLS): newest row in force, because after a message has gone out "what address was on file that day" is the question an audit asks and an update would destroy the answer; a null address is a withdrawal recorded rather than a row deleted, since a person who stops has decided something. The table holds one rule the rest of the product does not: a person sets their own address and nobody else's, enforced by a trigger against `app.user_id` rather than by the service, because an administrator who could write another person's address could redirect that person's notices silently — and the notices are exactly the signal that something has gone unattended. The chain event names the act and never the address. Since Increment 1.59 the product sends, and the half that matters is the failure. Delivery goes through a **port with no default**: a practice that has configured nothing gets a transport that refuses in words naming the setting that would change it, because a silently discarded message is the worst possible default — the reader would believe they had been told. `notice_sends` (migration 0042, append-only under FORCE RLS) records one row per attempt with one of exactly three outcomes, and the database refuses a fourth: `sent` (the transport took it), `failed` (it refused, and `detail` carries its own words), and `unreachable` (something was owed and there was nowhere to send it, because nobody has given an address or the person withdrew — not an error, and not nothing). Check constraints hold the shapes: a failure that does not say what failed is refused, a send claiming to have gone nowhere or said nothing is refused, and an unreachable row carries no address and says why. **Nothing owed writes no row at all**, since that is not an act and a table of rows saying nothing happened is a table nobody can read. The body is kept rather than hashed, because "what did they actually receive" cannot be re-derived later from rows that move, and it is safe to keep for the reason Increment 1.58 settled — a live case asserts at rest that every stored body carries the line saying it names no patient and quotes nobody. The address is copied onto the row rather than joined through time, so it is the address this message actually went to. A person sends their own and nobody else's, on demand rather than on a schedule: a schedule needs a decision about what "since you last read" means when nothing records a read, while asking for it answers the question somebody actually has before relying on any of this. The chain event names the act and its outcome, never the address and never the body. Since Increment 1.60 a failure is not only visible but actionable. Increment 1.59 gave every refusal the same reading, so an owner facing a provider that was busy for a second and an owner facing an address that does not exist were told the same thing and offered the same non-choice; one of them should ask again and the other never should. The **transport now says which kind of refusal it gave**, because only the transport can know: `transient` where the provider was unavailable, busy, or timed out, and `permanent` where the address does not exist, the provider rejected the message itself, or the deployment has no transport at all — which makes an unconfigured practice permanent by the plain meaning of the word, since no number of retries configures a transport. `sendNotices` acts on the answer: up to three attempts against a transient refusal, with stated pauses of 500 ms and 1,500 ms held in one array the attempt count is derived from so the two cannot disagree, and exactly one attempt against a permanent refusal. **The retrying leaves rows, not a counter**: every attempt writes its own row before the next begins, so how many times the practice tried is answered by counting rows rather than by a number beside them the rows could contradict — which is both the shape this product has refused since the owner board's counts and the queue this increment is careful not to become. Each attempt stamps its own moment, since attempts inside one act are seconds apart, and the chain carries one event for the act with the attempt count on it, never one event per attempt, because a person asked to be sent their notices once. Migration 0043 adds `failure_kind` with three checks: two kinds and no third, a kind only on a failure, and — **NOT VALID** — that every failure says which kind it was. The last is deliberately not validated against existing rows: a failure recorded before the distinction existed genuinely does not know its kind, and filling one in would invent a fact about somebody's past attempt, while guessing `permanent` would tell a reader not to try again on evidence nobody ever had. Those rows keep a null and read exactly as they read under 1.59; every failure recorded from here on must say. The outcome sentence carries the advice the kind licenses — that a refusal of this kind can pass, so asking again later may get through, or that it will not, so asking again would fail the same way and something has to change first — and the clause is true of the row alone, in any position, because it reads the kind rather than how many attempts surrounded it. The delivery section states the standing rule beside the outcome it governs, since a reader deciding whether to press the button again deserves to know what pressing it already did. Since Increment 1.61 the product no longer takes the practice's word for where a message goes. Increment 1.58 stopped one person redirecting another's notices; it did not stop a person redirecting their own into a typo, and the typo is the likelier accident. `riley@ridgeveiw.example` passes every shape check there is, and the guarantee Increment 1.59 built does not help, because **a message a stranger's mailbox accepts does not fail** — it succeeds, silently, which is the one outcome this whole arc exists to prevent. So a code goes to the address and the person brings it back, and the coming back is the proof. **A code rather than a link**: a link is a state-changing GET that puts a bearer secret in a URL a browser keeps, a proxy logs and a referrer leaks, while a code carried from the inbox to a screen the person is already signed into proves both halves at once — the token proves who can open the mailbox, the session proves who is asking. The word is **proved** rather than verified, because `packages/verifier` verifies the hash chain and one word for two concepts is how a reader ends up believing a claim nobody made. Migration 0044 adds two append-only tables under FORCE RLS, split because a secret and a fact are not the same thing: `notice_address_challenges` holds a code as its SHA-256 (never in the clear, since it went out in a message) with a 24-hour window, and `notice_address_proofs` holds the fact, which has no secret in it and never expires. **The proof names the address row, not the person** — so changing an address writes a new row that no proof points at, and a changed address is unproved by the shape rather than by anything remembering to clear a flag, which is this codebase's rule that a signal which never clears is not a signal, met for once without any clearing to do. The database holds the rest: one proof per address (which is what makes a code single-use), a composite foreign key so a proof answers a code issued for that same address, another so the address belongs to that same person, a trigger refusing a proof stamped after its code expired (reading the proof's own stamp rather than the clock, so it says what it means), and the same own-user trigger `notice_addresses` carries, which fires before every other check. Until somebody proves an address, `sendNotices` records `unreachable` with a reason naming what would change it: an address nobody has proved is not a destination, and the third way of having nowhere to send joins nobody-said-where and you-withdrew rather than inventing a fourth outcome. The code's own message goes out through the same transport, the same retry rule and the same row per attempt as anything else — its row carries `notice_count = 0`, which names it uniquely because a send of no notices never happens — so a code that could not be delivered is as visible as any other failure. The code stays out of the subject line for the reason Increment 1.58 gave about subjects, and off the chain entirely, which records only that a code was sent and that an address was proved. Since Increment 1.62 the arc finally does its job without being asked. Everything before it waited for somebody to press something, which is backwards: a notice exists to reach a person who is **not** looking. `pnpm notices:round` runs one round per practice — tenants listed on the administrator connection, each round then run on the runtime connection with that tenant bound, exactly as the nightly snapshot job beside it does, and a failure in one practice never stops the others. Of the two questions Increment 1.59 deferred, only one turned out to be a question. **"Since you last read" dissolves**: what a seat owes is derived on every read (Increment 1.57), so it is a state and not a feed, and a message needs to say what is true now rather than what somebody has seen — nothing records a read because nothing needs to. What is real is **repetition**: the same three sentences every morning is noise, noise gets filtered, and a filtered signal is not a signal. So a round sends only where the message **would read differently from the last one that actually reached that person**, comparing body against body rather than a digest kept beside them, since what was sent is one fewer thing that can drift. Staleness is the other half and is not optional: change alone would mean a debt nobody acts on is mentioned once and then never again, and silence reads as "nothing owed", so the thing most in need of attention would be the quietest — a message that still stands therefore goes again each week. Three properties fall out rather than being built. **Running it twice changes nothing**, because a second round finds every message unchanged, so the schedule may be early, late, or doubled by two workers without a person being told twice — which is what lets a practice schedule it with whatever it has rather than needing something exact. **A failure is not "told"**: the comparison reads the last send that actually left, so a message the transport refused does not suppress the next attempt, and Increment 1.59's visible failure is not also a silencing one. And **somebody who withdrew is not considered at all**, since counting them among the people a round could not reach would file a decision as a failure. Every round writes one row to `notice_rounds` (migration 0045, append-only under FORCE RLS) **including the quiet ones** — the single place this codebase writes a row saying nothing happened, and for the reason that decides the whole increment: a round that wrote nothing when it had nothing to do would make a scheduler that died indistinguishable from a practice that owes nothing, and telling those two apart is the entire job. A check constraint refuses counts that do not account for everybody considered, the Practice Risk page says when the sender last ran, and the chain event names **nobody** as its actor, because nobody did it — `appendControlEvent` now admits a null actor, which the column always did. Since Increment 1.63 there is a limit on how often the product can be made to send, and deliberately none on guessing. Increment 1.61 left a guessing throttle as work to come; on inspection it protects nothing. The lookup is scoped to the caller's own rows, the only address anybody can prove is their own, and somebody who wants that outcome can press the button and be sent a code — so guessing buys nobody anything they cannot have for the asking, and a throttle there would be a thing that looks like protection. The real abuse is the other half: a signed-in person can point this product's mail at **somebody else's address** by typing it and asking again and again. So five asks an hour, **per person rather than per address row**, because a per-row limit is escaped by changing one character and starting a fresh allowance — which is exactly the move being limited. The count comes from `notice_address_challenges` itself, which is append-only and already records every ask, so the rows are the count: no counter, nothing to reset, and nothing that can disagree with what happened. That is why this does not reuse `auth_throttle`, whose mutable `fail_count` earns its keep against unauthenticated sign-in traffic that must not be allowed to write a row per attempt, and would be a status column here. The refusal is a 409 naming the time they may ask again rather than telling somebody to try later and leaving them to guess, it is checked before anything is minted or written so a refused ask spends none of the next window, and the delivery section says the limit where a person about to press the button will read it. Since Increment 1.64 the weekly digest is delivered, and it is emphatically **not a second digest**. Increment 1.28 built one — seven days of the practice's counts, computed from rows, stamped once by the owner — and it is already weekly and already the practice's retrospective; what was missing was that reading it needed somebody to remember to look, which is the gap the whole notices arc exists to close. It is safe to send for a property the product had already proved for another purpose: every count is practice-wide because the queries carry no person dimension, settled in 1.28 so that the hash the owner stamps could mean what it says, and that is exactly what Increment 1.58 requires of anything leaving the product. The message carries **counts and no money**, because an amount in an inbox is the one figure a person who should not have it would find worth reading, and counts already say whether a week needs attention. It rides the same round as the notices **on a different rule**: a digest answers "has a week passed?" while notices answer "has anything changed?", and since the digest's counts move every day the change-or-stale rule would turn it into a daily message — precisely the noise that rule exists to prevent. So: one digest per completed week per person, the week being the seven days ending on the most recent Sunday (a fixed boundary, since a rolling one would name a different week each day and a subject that changed daily would be a new message daily), and "once" answered by the message itself — the last digest that reached this person is older than the week now being reported, or there has not been one — so no column records which week was sent. It goes to the owner seat only, since the outside accountant's seat reaches the month-end package and nothing else. **Reading it is not stamping it**: the message says so in those words, and the acknowledgment stays an act on the digest screen, because a message that implied otherwise would quietly retire a control. Migration 0046 gives `notice_sends` a `kind` — notices, proof_code or digest — so a reader is told what a message was rather than inferring it from a `notice_count` of zero, with the earlier rows **backfilled rather than grandfathered**: Increment 1.60 left `failure_kind` null on old rows under a NOT VALID check because that fact was unknowable, while this one is knowable from a rule the database already held, so recovering it is reading a fact rather than guessing one, and a check now holds that only a code carries no notices and a code always does. `notice_rounds` gains `digests_sent` and `digests_failed` beside the sum that must add up rather than inside it, because `considered = sent + failed + unchanged + nothing_owed + unreachable` says every person became exactly one **notices** outcome, and folding a second message into it would make the invariant say nothing. Since Increment 1.65 a proof no longer outlives its evidence. A proof was forever, which quietly reintroduced the failure Increment 1.61 exists to prevent: a mailbox somebody loses access to — they leave, the provider changes, a shared inbox is reassigned — stayed proved, and the notices kept arriving where nobody reads them, which is the same silent success as a typo merely delayed. A proof now **stands for a year**, a decision about the practice rather than about the code and the product's default rather than a law: long enough that nobody is nagged, short enough that a mailbox nobody holds any more is caught within a plausible turn of staff. It is **derived, never stored** — `notice_address_proofs` already dates every proof and never changes one, so when a proof lapses is arithmetic on a row that cannot drift, with no expiry column to keep in step and no job that must remember to run. The standing has four states rather than two, and the interesting one is `expiring`: a product that knew only proved-or-not would have nothing to say until the day it stopped sending, and stopping without warning is the silence this arc refuses. So the round asks for a new code inside the last thirty days, once per window, and a **lapsed proof refuses exactly as a never-proved one does** rather than earning an outcome of its own — what differs is only the reason, because "nobody ever said" and "nobody has said lately" call for different things from the reader. Two rules moved to where they belong. Migration 0047 takes **single use off the address and onto the code**: one proof per address meant "a code is used once" only while a proof was forever, and the moment a proof can lapse it means "an address can never be proved twice", so the unique index now sits on the challenge, which is the thing the guarantee is actually true of. Migration 0048 splits the own-user rule along the line it was always about: a **proof** stays strict and must be written by the person it names, while a **challenge** may also be written with no acting user at all, which is the product acting for itself — a challenge sends a code to the address already on file, confers nothing, and reaches nobody the person did not choose, and every route runs under a guard with a real user, so "no acting user" is reachable only from a job the practice runs. Asking the round to borrow somebody's identity instead would have put a lie in the one column the rule is enforced against. The limit on asking (Increment 1.63) does not bind the round for the same reason: that limit exists because a person can type a stranger's address, and the round only ever writes to an address this person already proved. And a **person who has left the practice is sent nothing at all** — a defect until now, since the round read `users` without ever checking `active`, so a deactivated account kept receiving the practice's notices at an address nobody had revisited; they are not counted as unreachable, because that count is about people a round could not reach and this is a person it must not reach. Since Increment 1.66 the outside accountant is told when a month closes. That seat reaches the month-end package and nothing else (Increment 1.49) and learned of a close only by signing in to look: the practice closes a month, and the person whose whole work begins at that moment is not told. The message carries the month, the **package fingerprint** and the shape it was taken under, how many entries the month held, and whether each tie-out holds — and **no money at all**. That line is not the digest's repeated for its own sake. The package *is* money; it exists to carry the practice's figures to their accountant, who may already export the whole thing as CSV or JSON. But an export is **the accountant taking the figures while signed in**, and a message is **the product pushing them into a mailbox** it has only proved reaches somebody — different acts with different blast radii, and a month of revenue is the most commercially sensitive number this product holds, while a link costs nothing. The fingerprint is the reason to send anything at all: a hash travelling by a different channel from the artefact it describes is the oldest integrity check there is, so the accountant can compare what they later download against what was closed without trusting the channel it arrived on, and a hash of figures is not the figures. The message names nobody, not even who closed the month; a failing tie-out is named, but its detail carries figures and stays behind the guard. It goes once per close, answered by the message itself as the digest's is — the last package message that reached this person is older than the close now being reported — so no column records which month was told. Migration 0049 widens `notice_sends.kind` to four and gives `notice_rounds` a third axis beside the sum that must add up. **A defect this increment found in the round**: a seat that owed nothing left the loop outright, which silently took the digest and the package with it — and owing nothing is the ordinary case for both, so an owner who owed nothing had been getting no weekly digest since Increment 1.64. A decision about the notices now ends only the notices. Since Increment 1.67 the person on the other end of an unwanted code can stop it. Increment 1.61 built the proof because a signed-in person can point this product's mail at an address that is not theirs, and Increment 1.63 limited how often they may do it; neither gave the reader anything to do, and the message ended by telling them to ignore it — advice that works for the product and not for the reader, since ignoring it stops nothing and the next one arrives anyway. The message now carries a **second secret with the opposite power**: the code proves an address and is carried by hand into a signed-in screen, while the stop secret travels in a URL a browser keeps and a proxy logs and can only withhold, so Increment 1.61's refusal to put a code in a link does not bar this one — that argument was about what the thing in the link could do. The link opens `/notices/stop/<practice>.<secret>`, the only page in this product outside a session: it **reads and does not act**, because mail scanners fetch links before a person reads them, and a form on it posts to a server action, which keeps "every route under `/api` passes through `withGuard`" true of the whole surface. The page names the practice and never the mailbox, so a reader holding only a leaked URL learns nothing they did not have. The link carries the practice's id because a database that refuses cross-practice reads cannot be asked which practice issued a secret; the id says whom to ask and the secret is what the answer depends on, and an unknown secret and one aimed at the wrong practice are answered in the same words rather than becoming an oracle. Migration 0050 stamps the secret's hash on the challenge whose message carried it (nullable, because a challenge issued earlier carried no link and no value written now could change what that message said) and adds `notice_address_refusals`: **the one row here written by nobody**, keyed to the address text rather than to a user or to an address row — a user id would name the wrong person, and a row id would be escaped by changing one character and saving again. It is scoped to one practice rather than to the product, because a cross-practice list would be the only table outside the isolation every other table has and would let one practice's reader learn that another mails the same mailbox. Folding case happens in `lower()` in the migration alone, and the application compares through the same expression rather than folding in its own language. A trigger refuses a refusal naming a mailbox the message never reached; a second trigger refuses a new `notice_addresses` row naming a refused mailbox, so the refusal bites where the row is written and not only where a message would leave. A refusal outranks a proof that still stands, on the screen and in `sendNotices` alike: a mailbox can change hands, and the person reading it now is entitled to say so. It is append-only and **not undone** — the only evidence that could lift it is a code sent to that mailbox, which is the one thing this practice may no longer send there — so the product loses a destination rather than a person: a different address is saved, proved, and the notices resume. The stop link works for thirty days rather than the code's twenty-four hours, derived from the challenge's own stamp and stored nowhere, because a code's window is about acting promptly while a refusal rests on the message having been unwanted, which stays true while the reader is away. **A defect this increment's browser suite found**: the server action first shipped exporting its initial state beside itself, which a `"use server"` file may not do — `tsc` and `next build` both accepted it and the page answered 500 on its first load, so the state moved to `stopFormState.ts`, exactly as `loginFormState.ts` sits beside `loginAction.ts`. **A second defect, found by CI rather than by that build**: two attempts at one send came back in the wrong order, and the cause was `uuidv7` filling its random bits with nothing but randomness, so two ids minted inside one millisecond ordered at random — a 10,000-pair check put the pair the wrong way round 4,888 times. Every read here that asks for the latest row breaks its tie on the id, so `lastSend` could report a send that worked as a send that failed. The twelve bits RFC 9562 reserves as `rand_a` now carry a counter, seeded randomly when the millisecond changes and incremented while it repeats, with two limits stated rather than papered over: the counter lives in one process, and a caller stamping an earlier millisecond reseeds because that id belongs in that earlier range. Increment 1.61 had already fixed an unordered `LIMIT 1` for being a coin toss; the tiebreak those ordered queries fall back on was the same coin toss one level down. Since Increment 1.68 the round keeps asking after a proof lapses, and then lets the address go. Increment 1.65 had it ask inside the proof's last thirty days so nothing stopped in silence, and then **stopped asking the moment the proof lapsed, forever** — which left the person who missed one message with no way back, because the one thing that would tell them to fetch a code is the notices being withheld. The round now asks **monthly, three times**, spacing from the last code that actually reached the transport so that Increment 1.65's ask inside the window and the asks after it are one cadence rather than two rules meeting at a cliff; only a **sent** code counts, since charging a person for one the transport refused would retire an address because the product could not send. The last code keeps its month, because a code answered on its twenty-ninth day is answered, and answering at any point restores everything — a code brought back is a fresh proof and the notices resume. **Retirement is derived and stored nowhere**, so this increment adds no migration at all: when an address stops being a destination is arithmetic over rows that cannot change, a column would be a status the rows under it could contradict, and the date a reader is given on day one is the date it happens. "Lapsed" had been covering two states that call for different things from the reader, so the send record and the screen now say which — still being chased, with the count and the day it stops, or stopped, with the count that went unanswered. Asking again is safe only because of Increment 1.67: every one of these codes carries a stranger a one-click way to stop it, and a refused address is refused before a code is minted, so the increment that gave strangers a way out is what licenses the product to keep trying. Nothing is written on the person's behalf — `notice_addresses` still refuses a row acted for anybody but its owner, and retirement deliberately avoids needing that relaxed, since a product that could write a null address for somebody could silence their notices, which is the attack Increment 1.58 exists to prevent. Since Increment 1.69 the practice can see who it cannot reach. Increment 1.68's asking is the only telling, and it goes to the mailbox that is failing — so a practice could run for months believing all of its people were being told while one of them was not. A **standing reading**, derived on every read and stored nowhere, names everybody with an address in force that the product cannot reach, and which of four things is wrong: **refused** (1.67, the mailbox's own word), **retired** (1.68, the codes ran out), **lapsed** (the practice is still asking, and the sentence says when it stops), or **unproved** (nobody ever brought a code back). The states rank and the strongest is reported, which a live case pins by refusing an address whose proof has not lapsed and checking the reading still says refused. A change feed was rejected: naming somebody once and then going quiet would mean the longer they had been unreachable the less the practice heard, which is the decay this arc refuses. It reports nobody whose silence is a decision or a rule — a withdrawal (1.58) leaves even the denominator, and a person who has left (1.65) is somebody the product must not reach rather than cannot. It **names people and never addresses**, since where somebody is reachable is theirs, and it shows in every state including the quiet one, carrying how many addresses are on file so a zero reads as measured rather than as missing (1.52's rule). It appears on the owner's board and **beside** the weekly digest, never inside it: 1.53's reason (a standing figure would move every closed month's hash), plus one 1.53 did not need — the digest is also a message that leaves the product and names nobody, so a reading that names people lives only where a guard stands. The board and the digest quote one pure sentence function, and `seatOf` moved from the round to `outstanding.ts`, where a seat is defined, rather than being copied for a second caller. Since Increment 1.70 it also sees who it was **never set up** to reach. 1.69 reads only people who have an address in force, so a person who never gave one was invisible: nothing failed, nothing was refused, and nobody learned anything. The new reading is scoped to the people who could act on what a notice says — derived from the guards rather than from a rank written down twice, since every notice names where the doing happens (1.57) and `NOTICE_PLACES` names those screens, pinned by a unit case over every notice `outstandingNotices` can produce. So the owner, any manager and the outside accountant are counted; the front desk and a new hire are not, which is what keeps this a card rather than the roster. Having an address stays anybody's to choose (1.58) — this reports only the converse, somebody the practice needs to reach and cannot. A withdrawal is still a decision rather than an absence, so it is named in its own list with what the decision costs rather than filed as a fault or passed over in silence; somebody who has left is in neither list nor the denominator (1.65). The two readings divide at the moment somebody saves an address: before it this one names them, after it 1.69's does, proved or not, which a live case pins as a handover. The denominator is the people who could act and does not move when the numerator does, so the card does not say less the better the practice gets. Since Increment 1.71 the practice can **create** that seat. The question was asked as an address for somebody who is not a user; the answer is no, and the foreign key is not the reason — a destination with no person behind it would be a second kind of recipient beside the seat, needing its own proof (1.61), its own stranger's refusal (1.67), and its own place in both readings (1.69, 1.70), which is four second answers to questions the seat already answers. A firm's shared mailbox wants to *be* the recipient, and the product's word for a recipient is a seat (1.49); what was missing was that `users` was written by the seed and by nothing else, so a practice that wanted an outside accountant could not have one. The owner now invites that seat and no other — it pairs with no duty in the SoD rulebook, so inviting it creates no conflict, while a general invite would be a grant path around `evaluateGrant`. **The practice names the seat; the person supplies the secret**, which is 1.58's rule read the other way: the row carries a hash of thirty-two bytes nobody kept, so the account exists and no password opens it until its holder sets one, proved by a live case checking that neither the invitation secret nor the username opens it. The ask and the answer are two tables (migration 0051) rather than a `claimed_at` column that could be rewritten; a trigger requires the acting user to be the inviter named, and the claim deliberately asks nothing about the session because the person claiming has no account yet. The link works for seven days against a stop link's thirty — it opens an account rather than withholding one — is spent once, and is shown once, because the rows keep only a hash. Increment 1.72 then walked that seat's first sign-in and found it could not finish one: the MFA enrolment route opened at `user` rank, and the seat is `readonly` **by construction** (`isCpaSeat` is "holds the reporting grant and does not meet `user`"), so the one seat a practice can invite was the one the route excluded. Because the middleware sends every unenrolled session to `/enroll-mfa`, that was not a withheld screen but a lockout: the seat reached the only page it could and met "You do not have access to this action" with no way forward. Nothing had exposed it because the only `readonly` account in the fixtures was seeded already enrolled. Enrolling one's own second factor is not an action rank should weigh — it is how an account becomes usable, it acts on nobody else, and a rank that gates it gates signing in — so both handlers now sit at `readonly`, the lowest rank there is, with `requireMfa: false` unchanged as the thing that admits an unenrolled session; `seats.test.ts` reads the route file rather than asserting about it. The same walk put an axe audit on the enrolment screen for the first time and found a serious violation of long standing: the setup URI sat in a horizontally scrolling block that was neither focusable nor named, so a keyboard user could not read the one string the screen exists to hand over. A seat-aware landing after sign-in was written and reverted: it would have replaced a plain-worded refusal with a silent bounce, and a person who types `/home` is owed the explanation. Increment 1.73 then closed the last hole in the invite path: a lost or expired link. `inviteAccountant` has one exit that creates anything and it always creates a **new** user, and a repeat of the same seat was refused by the username check before the unique index was ever reached — so there was no re-invite path at all, and a practice's only recourse was a second seat, leaving the first active, holding the grant, openable by nobody, and named on 1.70's "never set up" card forever. The seat is fine and only its secret is stale, so the act mints a new secret and writes nothing else; a live case pins the same `userId` and username coming back with a different secret. **Newest row in force, and nothing marks the old one** — the shape `notice_addresses` has held since 1.58 — so migration 0052 drops `seat_invitations_one_per_seat` and the superseded link is refused by the read that finds it rather than by a flag, from both the lookup and the claim. A seat already opened is refused (that is password recovery, not a new link), and somebody who was never an invited seat is refused outright. The panel lists the seats waiting to be opened, one live link each. Since Increment 1.44 the package carries a sealed-days section of its own (`package-v3`): how many day closes the month froze, a row per sealed date a posting landed against with the count, the first postings among them, and the net, plus a tie-out row "No row posted against a day this month after the practice sealed it". The section is windowed on the sealed day rather than on the posting, so a row posted in a later month against one of this month’s sealed days appears here and not in this month’s journal — which is exactly the row an accountant reconciling daily slips against the journal would otherwise hunt for. The CSV export carries the section and its denominator; nothing in it names a person. Since Increment 1.45 the practice governs its own reason codes at `/reason-codes` (anyone who posts may read them, the administrator changes them): `ledger_entries.reason_code` is a foreign key into `reason_codes (tenant_id, code)`, so the code never changes and a code in use is retired rather than removed — retiring clears `active`, takes it off the forms, and leaves every entry citing it readable, which is the only thing the key permits and the only honest treatment of history. The label is free to change because nothing keys on it, `prior_period` may be relabelled but never retired (the closed-month refusal admits a correction only under it), and each of adopt, relabel, retire, and restore is a chain event. The posting and correction forms are built from the rows rather than from a constant, and the screen shows how many entries cite each code, which is the reason a code is retired rather than deleted. The table needed no migration for that: it has carried `active` and FORCE RLS since migration 0010. Since Increment 1.46 a reason also carries its own dual-release threshold (migration 0035 makes `requires_approval_over_cents` nullable so its three states are distinguishable: null = the channel’s figure governs, 0 = every posting under that reason waits for a second person, N = a second person above N cents; every existing row backfills to null, which is the truth about rows nothing had ever read). The effective threshold is `least(channel, reason)`, so a reason only ever **tightens** — one that could loosen would let a practice undo dual release by inventing a reason. The database enforces it in `ledger_entries_requires_approval` (the whole current function carried forward, after-hours hold and correction branch included, with `GRANT SELECT ON reason_codes TO app_append` for the held-release path) and the posting and correction services tighten the policy they hand the engine by the same rule, so the practice meets a hold in words rather than a crash. A `raise_threshold` exception is untouched: it carries a decision and a review date, so it still licenses up to its own figure. Each change is a `reason_code.threshold_changed` chain event recording which way it moved. Since Increment 1.47 that direction decides what the change takes: tightening is a settings change, and loosening — raising the figure, or clearing it so the channel governs again — is a control decision, exactly as switching the after-hours hold off is (Increment 1.31). The service refuses a loosening without an accept-residual or compensate decision carrying a note and a review date (409: the figure was well formed, the practice’s state is what refuses it), records the decision and the change in one transaction with the chain event naming the decision that licensed it, and retires that decision when the threshold is tightened back, because the control stands again and nothing is left to accept. The decision’s subject is the reason code itself (migration 0036 widens `control_decisions.subject_kind`), whose key never changes, so it keeps pointing at the same reason; the owner board’s decisions-due card carries it like any other. The form holds Save until the decision is complete rather than spending a round trip to be refused. Since Increment 1.48 that refusal and the one for switching a tightening exception off read as one: both answer 409 in the same words, from a shared `needsDecision.ts` holding the status, the code, and the sentence, because both are the same act asked of two objects and both already reuse `decisionPermitsRetirement` | `apps/pms/src/lib/ledger/reasonCodes.ts`, `apps/pms/src/lib/ledger/reasons.ts`, `apps/pms/src/app/(app)/reason-codes/` | `packages/db/migrations/0032_late_postings.sql`, `apps/pms/src/lib/day-close/service.ts`, `apps/pms/src/lib/home/afterClose.ts`, `packages/db/migrations/0033_finding_sealed_day_posting.sql`, `apps/pms/src/app/(app)/day-close/page.tsx` |
