# Dental_Mgmt: consolidation of Smile Notes and Precog Pioneer into one dental practice management system

This repository holds the **review and plan** for combining two existing products into a single, HIPAA-grade, commercial dental practice management system (PMS):

- `CatCorner22/dental` (**Smile Notes**): a de-identified clinical-note builder with a deterministic audit engine, a hardened authentication layer, and 201 test files.
- `CatCorner22/precog` (**Precog Pioneer**): an internal-controls and residual-risk coach whose segregation-of-duties rulebook and dual-release evaluator become the PMS's enforced controls.

Phase 0 Increment 0.1 started the pnpm monorepo (`apps/pms`, `packages/*`) plus the planning corpus and clickable prototype. Increment 0.2 wires sign-in to the `sessions` table (`AUTH_DEV_MEMORY=1` for a synthetic two-tenant store; `POSTGRES_URL` for Drizzle). Increment 0.3 makes the database real: a migration runner with history, the cluster roles with grants, live RLS and tamper tests in CI, a chain-verifier CLI, and a production boot guard that refuses a connection able to bypass RLS. Increment 0.4 forces MFA enrollment on first login (`ridgeview-newhire` in the seed) and adds `pnpm db:seed` for Postgres dev without `AUTH_DEV_MEMORY`. Increment 0.5 routes ledger appends through `APPEND_ROLE_DSN`, adds `audit_chain_checks`, and ships `verify:chain:record` plus a nightly workflow template. Increment 0.6 adds the `disclosures` schema and a two-admin recovery ceremony API. Increment 0.7 anchors signed chain heads to Object Lock storage. Increment 0.8 wires disclosure egress accounting and tenant-wide session revoke. Increment 0.9 adds controls-engine golden snapshot tests. Increment 0.10 adds the restore drill CLI. Increment 0.11 adds the PHI-free metrics package and refuses `DEV_MFA_KEY` in production boot. Phase 1 Increments 1.1 and 1.2 add the ledger kernel and the approvals inbox; Increments 1.3 through 1.11 add Curve Hero import staging and apply, the Money Desk read-only, posting, and statements screens with the guarded posting route, bank statement import and reconciliation, deposits and atomic day close, the approvals inbox screen, and runtime SoD on variance clearance (migrations 0015 to 0018); Increment 1.12 wires Precog to live rows (SoD from real grants with grant refusal, the decision register, channel coverage with an enforcement class, exceptions as policy versions, frozen risk snapshots) under `/api/controls/*`; Increment 1.13 makes the database re-check dual release on every guarded ledger insert and adds the nightly snapshot job (`pnpm controls:snapshot`); Increment 1.14 runs dual release on the deposit path, so freezing a day close needs a second count by someone other than the preparer and the deposit channel moves from attested to enforced; Increment 1.15 adds the Practice Risk page (`/risk`): the enforced-versus-recorded table, duty combinations with their governing decisions, grant and revoke with the Refusal component, the decision register, exceptions, and the snapshot's assumptions; Increment 1.16 measures independent bank reconciliation from cleared reconciliation runs as a three-state grade (independent, same hands, stale import) and feeds it to the scores in place of a hard-coded absence; Increment 1.17 drives the Practice Risk page in Chromium against the production server in CI (`pnpm e2e`, after `pnpm --filter @pms/app build`) and fixes the same-origin check on guarded writes, which read Next.js's own listen address instead of the request's Host header and so refused every browser write behind a real hostname; Increment 1.18 drives the whole Money Desk the same way (ledger, posting and a held write-off, approvals, statement import and clearance, day close, statements) in twelve browser tests; Increment 1.19 matches bank credits to the deposits the practice prepared, measures detection lag and the 48-hour match rate from the bank lines (recorded on the snapshot and shown on the reconciliation screen and Practice Risk, not yet scored), and fixes the import of a statement a second time; Increment 1.20 runs axe-core over the states the browser suites reach and fails a suite on a critical or serious violation (none found); Increment 1.21 builds the owner's home board from live rows ("Yesterday reconciled?" as one shape and word with one action, approvals waiting, decisions due, expiring exceptions, practice health with its top three levers); Increment 1.22 adds `control_findings` (migration 0019) and the first detector, unmatched bank lines older than 48 hours, run on every frozen snapshot, closed with a reason when the line clears, and shown on Practice Risk; Increment 1.23 adds the owner-only clearance and decision-past-review detectors on the same planner; Increment 1.24 adds three ledger detectors (release above threshold without approval, backdated posting, duplicate patient payment) with migration 0020; Increment 1.25 adds two coverage detectors (a deposit not yet at the bank, a critical duty held by one person) with migration 0021; Increment 1.26 lets the owner record a control decision on a detector finding (migration 0022), refusing self-licensing by whoever the row is about, and shows the governing decision on Practice Risk and the undecided count on the home board; Increment 1.27 puts Keep, Tighten, and Retire on the home board's decisions-due card, each decision carrying a practice-wide sentence on what happened since it was made (migration 0023 adds the `retire` kind); Increment 1.28 adds the weekly digest at `/digest`, seven days of the practice's counts from rows with a once-only acknowledgment stamp (migration 0024, `digest_acks`); Increment 1.29 reads the six hard events (after-hours refund, retroactive-dated entry, waived dual control, deposit variance over threshold, chain-check failure, new device on a financial role) from rows onto the owner board, with business hours per location (migration 0025); Increment 1.30 turns the after-hours refund from an alert into a held posting: a refund, adjustment, or write-off posted outside the location's hours needs a second person whatever the amount, enforced at the service and by the database trigger (migration 0026); Increment 1.31 makes switching that hold off a governed change (an owner's accept-residual or compensate decision with a required review date, written with the policy version; the owner home reads "off since, review due" until it is switched back on, which retires the decision); Increment 1.32 adds the Locations page (`/locations`), where the owner sets each location's business hours, the week the after-hours hold reads, every change a chain event counted in the digest; Increment 1.33 lets the owner acknowledge each hard event with a note of what was done (`hard_event_acks`, migration 0027, append-only, one per event) and has the digest count acknowledgments and after-hours holds; Increment 1.34 ships the first slice of the CPA month-end package (`/cpa`): one calendar month, aggregate and PHI-minimal, hash-stamped, exported as CSV or JSON with every export a chain event; Increment 1.35 adds the chart-of-accounts mapping under maker-checker (migration 0028, `gl_mappings`: one person proposes, a different one approves, the database enforcing both), so each journal line names an account or reads unmapped; Increment 1.36 closes a month (migration 0029, `month_closes`: one append-only row freezing the package hash, closing refused while a line is unmapped, a closed month never re-opened) and has the database refuse any entry effective-dated into a closed month unless the correction carries reason `prior_period`; Increment 1.37 makes that refusal the pair rather than the label (migration 0030, `ledger_entries.corrects_entry_id`): correcting a posted entry writes a reversal mirroring it and a repost carrying the corrected figure in one transaction, the database refusing a reversal of a reversal, a second reversal of one entry, an unmirrored amount, and a repost with no reversal behind it, with Correct on the account ledger and each half saying which entry it reverses or reposts; Increment 1.38 lets one approval release the pair (migration 0031, `approval_requests.corrects_entry_id`), so a correction above the dual-release threshold waits as a single request and the second person's decision writes both halves, neither above the figure they approved; Increment 1.39 has the approvals inbox read that request as a correction (the entry it replaces, the figure now and the figure proposed, and that approving writes both rows while declining writes neither), and fixes three defects a browser drive found in merged code: a free-text reason reaching the `reason_code` foreign key as a failed insert, `prior_period` never seeded (so no practice could correct a closed month), and a correction form that took dictation where the posting form offered a list; Increment 1.40 makes a sealed day report what arrived behind it: a database trigger stamps every ledger row it admits against a frozen day close with `posted_after_close` and `closed_day_id` (migration 0032), reading the seal at insert time and overwriting whatever the writer passed, and the day-close page carries the lock beside "Frozen" and a "Since the seal" section naming each late row — first posting or half of a correction — with what they move the day by and the sealed figures left as the practice counted them; Increment 1.41 reads those stamps back onto the owner board as two practice-level counts — what landed behind yesterday’s seals, and what landed behind any seal in the last 30 days, each splitting first postings from halves of a correction — shown in every state, the quiet one included, because a pattern of late postings is visible only across days; Increment 1.42 turns those stamps into a ninth detector and a seventh hard event (migration 0033), so a first posting behind a seal opens a finding — medium alone, high once a second lands behind the same seal — and pages the owner one at a time, with both halves of a correction excluded because a correction names the entry it replaces; Increment 1.43 gives the month-end package a schema version (migration 0034, `month_closes.package_schema`), so a frozen hash says which shape it froze and the CPA page can tell "the package changed shape" from "a figure moved" — falling back to the entry count and journal total the close froze, which no schema change touches — and the weekly digest can finally count postings into sealed days without making every closed month report a change it never had; Increment 1.44 gives the month-end package a sealed-days section and a tie-out of its own (`package-v3`), naming each day of the month that moved after the practice sealed it — windowed on the sealed day rather than the posting, so a row posted in a later month against one of this month’s sealed days appears there and not in this month’s journal; Increment 1.45 hands the practice its own reason codes at `/reason-codes`: the code never changes and a code in use is retired rather than removed, because `ledger_entries.reason_code` is a foreign key into it; `prior_period` may be relabelled but never retired; every change is a chain event; and the posting and correction forms are built from the rows rather than a constant, which settled a disagreement the constant had been hiding about which kind `correction` belongs to; Increment 1.46 gives a reason code its own dual-release threshold (migration 0035): null means the channel’s figure governs, 0 holds every posting under that reason, and N holds above N cents — and because the effective figure is `least(channel, reason)`, a reason only ever tightens, since one that could loosen would let a practice undo dual release by inventing one; the database and the services compute the same figure, so the practice meets a hold in words rather than a crash; Increment 1.47 makes loosening one of those thresholds a control decision rather than a settings change (migration 0036): raising the figure or clearing it needs an accept-residual or compensate decision with a note and a review date, the decision and the change are written together with the chain event naming the decision that licensed it, and tightening back retires it because the control stands again; Increment 1.48 makes that refusal and the one for switching a tightening exception off read as one — both answer 409 in the same words, from a shared module, because both are the same act asked of two objects — and puts each reason's threshold on the Practice Risk coverage table under the channel figure it tightens, naming the decision that licensed any loosening, so a reader of that table is no longer reading a figure that stopped governing every posting on the channel; Increment 1.49 gives the month-end package the person it was built for — an outside accountant signs in at the lowest rank the product has, holding one reporting grant, reaches `/cpa` because a guard may now open a route on a grant in the rank's place, reads and exports the month, and meets a refusal on every other screen; the seat needs no business-associate agreement because the package names no patient, which a live case proves against the practice's own patient and guarantor rows rather than asserting in prose, and the header now offers each viewer only the screens its rank and grants open; Increment 1.50 makes the package two-way — the accountant opens a thread on a line the month states and the practice answers it from the owner board, append-only and on the chain, with who owes the next word read from the last message rather than from a status column; Increment 1.51 puts something behind the word "attested" on the coverage table — a dated assertion, by a named person whose seat is recorded beside it, that someone reviewed one of the two channels this build cannot enforce, refused on a channel it can, said once and never rewritten, and carried in the month-end package (which moved its schema to `package-v4`); Increment 1.52 says the other half — what nobody vouched for: the month-end package gains a tie-out row, "Every channel the product cannot enforce was reviewed by somebody" (`package-v5`), and the owner board carries the same reading as a card naming the channels nobody has reviewed for the month that has ended, shown in every state so a reviewed month is legible as one, both computed from one pure function over the same rows rather than from two sentences that could drift; Increment 1.53 carries that reading to the weekly digest, and splits it to keep the digest honest: the channels somebody attested inside the seven days are a count in the digest, hashed with it and stamped by the owner's acknowledgment (`package-v6`), while what the practice still owes for the month that has ended rides beside the digest and never inside it, because a standing figure in there would move the hash of every closed month the moment anybody attested anything; Increment 1.54 marks a question about a month the practice has since closed: the thread on `/cpa` and the card on the owner board both carry one sentence naming who closed the month, the day, and that a correction to that line now posts today under `prior_period` rather than moving a frozen figure, because read alike a closed month and an open one call for different answers; Increment 1.55 tells the accountant that an answer landed, and makes the telling clearable: reading a thread is a stamped act (`cpa_thread_reads`, migration 0039, append-only and deliberately not unique, so a later message re-opens the signal without any row being rewritten) rather than a badge that never clears, the database refusing a read that names another thread's message and the service refusing one where the last word is the reader's own; Increment 1.56 lets a month closed under an older package shape be compared again without rewriting what the accountant received: a baseline (`month_close_rehashes`, migration 0040, append-only, one per shape and never the close's own) records what the month hashes to under the shape in force now, so "has a figure moved?" is answerable again from the baseline's date rather than the close's, while the close's frozen hash stays exactly as it was; Increment 1.57 folds what each seat owes into one **What people owe** section on Practice Risk — unattested channels, questions waiting on the practice, answers waiting to be read, decisions past review — derived on every read and stored nowhere, because a notices table would be a status column that can disagree with the rows under it, with each entry carrying the sentence its own surface already writes and naming the seat that owes the doing; Increment 1.58 answers what would be **sent** and to whom, while sending nothing: a notice now carries an `outside` sentence beside the one the screen shows, because two of the four kinds quote words a person typed and a message leaves the product, where nothing constrains what those words say — the renderer's input type omits the quoted field, so the rule is held by the compiler rather than by care; the subject carries the practice and a count and never what is owed, since a subject is what a lock screen shows; no message at all goes out when a seat owes nothing; and `notice_addresses` (migration 0041, append-only, a null address a recorded withdrawal) holds where a message would go, with the database refusing any row whose acting user is not the person it names, so nobody redirects anybody else's notices; Increment 1.59 sends, and makes failing to send visible: a transport port with no default, so a practice that has configured nothing learns it cannot send the first time somebody asks rather than a message vanishing quietly, and `notice_sends` (migration 0042, append-only) records one row per attempt with one of three outcomes — sent, failed with the transport's own words, or unreachable when something was owed and nobody had said where to send it — while nothing owed writes no row at all, because that is not an act; the body is kept rather than hashed, since what somebody actually received cannot be re-derived from rows that move and carries no patient by construction; and a person sends their own and nobody else's. Increment 1.60 makes a failure actionable rather than merely visible: the transport says whether its refusal can pass, a transient one is tried up to three times in one send while a permanent one is tried once, every attempt still leaves its own row (migration 0043 adds `failure_kind` under a NOT VALID check, so every later failure must say which kind it was and no earlier row has one invented for it), and the outcome sentence tells the reader whether asking again could work. Increment 1.61 stops the product taking the practice's word for where a message goes: a mistyped address does not bounce, it is accepted by whoever does own that mailbox, so a code goes to the address and the person brings it back, and until somebody does the notices record `unreachable` rather than going out. Migration 0044 keeps the code as a SHA-256 and the proof as an append-only fact about **the address row**, so changing an address unproves it by the shape rather than by anything remembering to clear a flag. Increment 1.62 makes the arc do its job without being asked: `pnpm notices:round` runs one round per practice, sending each seat what it owes only when the message would read differently from the last one that actually reached that person, or when that one is a week old — which makes a repeat a signal rather than noise, keeps an ignored debt knocking, and makes a second round a no-op, so the schedule itself may be early, late or doubled without anybody being told twice. Every round writes a row (migration 0045), including the quiet ones, because a scheduler that died must not look like a practice that owes nothing. Increment 1.63 limits how often the product can be made to send: five codes an hour per person, counted from the append-only challenges rather than from a tally beside them, and per person rather than per address row so that changing one character is not a fresh allowance. The throttle on *guessing* a code that Increment 1.61 left as work to come is deliberately not built, because it would protect nothing — a person can only prove their own address, which they could prove by asking. Increment 1.64 delivers the weekly digest rather than building a second one: it already exists, is already weekly, and already names nobody — a property Increment 1.28 established so the owner could stamp a hash of it, and exactly what a message that leaves the product requires. It rides the same round on its own rule, because a digest answers "has a week passed?" while notices answer "has anything changed?", and its counts move daily; migration 0046 gives each send row its kind rather than leaving a reader to infer one from a count of zero. Increment 1.65 stops a proof outliving its evidence: one stands for a year, derived from the append-only row rather than stored, the round asks for a new code inside the last thirty days so nothing stops in silence, a lapsed proof refuses exactly as a never-proved one does, single use moves off the address and onto the code it is a fact about, and a person who has left the practice is sent nothing at all — which the round never checked. Increment 1.66 tells the outside accountant that a month closed, since that seat reaches the month-end package and nothing else and until now learned of a close only by signing in: the message carries the package fingerprint, the shape it was taken under, the entry count and whether each tie-out holds, and **no money** — an export is the accountant taking the figures while signed in, a message is the product pushing them into a mailbox, and the fingerprint travelling by a different channel is the point, since it lets them check the package they download against the one that was closed; see `docs/05` for the status table. Increment 1.67 gives the person on the other end of an unwanted code a way to stop it: the message now carries a second secret with the opposite power — the code proves an address and is carried by hand into a signed-in screen, while the stop secret travels in a URL and can only withhold — opening the one page in this product outside a session, which reads and does not act because mail scanners fetch links before people do, with a server action behind the button so every route under `/api` still passes through `withGuard`; migration 0050 stamps that secret's hash on the challenge whose message carried it and adds `notice_address_refusals`, keyed to the address text rather than to a user or an address row, scoped to one practice, append-only and never lifted — the only evidence that could lift one is a code sent to that mailbox, which is the one thing a refused mailbox may no longer be sent — so a refusal outranks even a proof that still stands, a refused address cannot be saved again, and the practice loses a destination rather than a person; CI on that increment also found a defect nothing local had: `uuidv7` filled its random bits with nothing but randomness, so two ids minted inside one millisecond ordered at random, and every read that asks for the latest row breaks its tie on the id — which meant `lastSend` could report a send that worked as a send that failed; the twelve bits RFC 9562 reserves as `rand_a` now carry a counter, seeded when the millisecond changes and incremented while it repeats; Increment 1.68 closes the trap Increment 1.65 left — once a proof lapsed the round stopped asking forever, leaving the person who missed one message with no way back, since the notices that would tell them to fetch a code are the notices being withheld — so the round now asks monthly and three times before letting the address go, spacing from the last code that actually reached the transport and counting only sent codes, with the last one keeping its month and any answer restoring everything; retirement is derived from those append-only sends rather than stored, so the increment adds no migration, and the send record and the screen now tell a lapse still being chased from one the practice has stopped chasing; Increment 1.69 tells the practice what only the unreachable person could see before — 1.68's asking goes to the mailbox that is failing, so a standing reading now names everybody with an address in force the product cannot reach and which of four things is wrong (refused, retired, lapsed, unproved), ranked so the strongest reason is the one reported; it reports nobody whose silence is a decision or a rule, names people and never addresses, shows in every state with the number of addresses on file so a zero reads as measured, and sits on the owner's board and beside the weekly digest rather than inside it — both because a standing figure would move every closed month's hash and because the digest is a message that leaves the product and names nobody. Increment 1.70 answers the half 1.69 left open — who the practice was never set up to reach at all: a person who never gave an address failed nothing, refused nothing, and told nobody, so the new reading names them, scoped to the people who could act on what a notice says (the owner, any manager, and the outside accountant, derived from the guards each notice's screen already carries rather than from a rank written down twice) so that it stays a card rather than the roster; a withdrawal keeps its own list because it is a decision rather than an absence, somebody who has left is in neither list nor the denominator, and the two readings divide at the moment an address is saved. Increment 1.71 lets the practice create the seat it had no way to add: the request for an address belonging to somebody who is not a user is refused on the ground that a destination with no person behind it would be a second kind of recipient beside the seat, while a firm's shared mailbox can simply hold the outside accountant's seat — so the owner now invites that seat and only that one, naming it while the person invited supplies the password (the row carries a hash of bytes nobody kept, so no password opens the account until its holder sets one), over a link that works for seven days, is spent once, and is shown once because the rows keep only a hash. Increment 1.72 walked that seat's first sign-in and found it could not finish one — the MFA enrolment route opened at `user` rank while the seat is `readonly` by construction, so the one seat a practice can invite met "You do not have access to this action" on the only screen an unenrolled session may reach; both handlers now sit at the lowest rank there is, since enrolling one's own second factor is how an account becomes usable rather than an action rank should weigh, and the same walk found and fixed a scrollable setup URI no keyboard could reach. The owner confirmed the recommendations in `docs/10-decisions-for-owner.md` for ADR-0002 (money and controls first as GTM) and ADR-0012 (one-way SuperByte; Byte, knowledge base, and cage in Phase 0; LLM provider call still Phase 5). Remaining ADRs stay Proposed. Owner-only Phase 0 items (BAA, hosting contract, 24-month budget, D.8 interviews) are not done. See `docs/17-competitive-enhancement-review.md`.

```
pnpm install
pnpm typecheck
pnpm test:packages
pnpm check:routes
```

`pnpm test` still runs the clickable prototype harness (syntax, docs, proto-check, axe, reproduce). `pnpm run ci` is the package gate.

### Database

Migrations are plain SQL under `packages/db/migrations`, numbered `NNNN_name.sql` without gaps, applied by the runner in `packages/db/src/migrate.ts` and recorded in `schema_migrations` with checksums. A recorded file that changes refuses to run; add a new file instead. Roles are created once per database by `packages/db/sql/roles.sql`; grants live in migrations.

```
POSTGRES_URL=postgres://pms:pms@localhost:5432/pms pnpm db:roles
POSTGRES_URL=... PMS_MIGRATE_ROLE=app_migrate pnpm db:migrate
POSTGRES_URL=... pnpm db:status
POSTGRES_URL=... PMS_VERIFY_ROLE=app_verify pnpm verify:chain
POSTGRES_URL=... pnpm db:seed
```

After migrate and seed, point the app at an `app_rw` login. `ridgeview-newhire` / `Dev-password-0.2!` signs in with password only, enrolls at `/enroll-mfa`, then signs in again with TOTP. Enrolled seed users (`ridgeview-owner`, etc.) still need an authenticator or recovery code.

The application connects as a LOGIN role that inherits `app_rw`. In production the boot guard refuses a superuser, BYPASSRLS, or table-owning connection. Live tests (`pnpm test:live`, or any `pnpm test` with `PMS_TEST_POSTGRES_URL` set to an administrator connection) build a throwaway database per suite, apply every migration as `app_migrate`, and prove the RLS negative case, the append-only grants, the BAA trigger, sign-in through the Postgres store, and tamper detection. CI runs them against a Postgres 16 service.

## Read in this order

| Step | Document | What you get |
|---|---|---|
| 1 | `docs/01-product-vision-and-scope.md` | Why the product exists, the market evidence, the three structural bets, the 22 design principles, and the module map with phase tiers |
| 2 | `docs/00-review-of-dental-and-precog.md` | What was verified in each repository, and what to lift verbatim, rewrite, or drop, file by file |
| 3 | `docs/02-architecture.md` | Stack, monorepo layout, database and tenancy, migrations, hosting, sessions, API, and the code-migration map |
| 4 | `docs/03-data-model.md` | Every entity group, the append-only ledger, and the database-enforced invariants |
| 5 | `docs/04-ux-blueprint.md` | Information architecture, one home screen per persona, the five daily flows with click counts, and how "intuitive" is measured |
| 6 | `docs/05-internal-controls-module.md` | How Precog's engines run on live PMS events, where controls refuse and where they only record |
| 7 | `docs/06-security-and-hipaa-plan.md` | 18 regimes, 36 controls mapped to tables and phases, architecture, identity, encryption, audit, AI policy, vendors and BAAs, backup, breach clocks, secure development |
| 8 | `docs/07-compliance-program-and-calendar.md` | Governance, cadence, attestations, the compliance calendar, and 32 questions for counsel |
| 9 | `docs/08-roadmap.md` | Six phases with scope, exit criteria, dependencies, sequencing contingencies, and the risk register |
| 10 | `docs/13-innovation-and-intuitiveness.md` | The feature layer: the thesis, six signature moments, 30 catalog features with specifications and click counts, 8 features adopted from the critique, the amendments adopted, and the ideas rejected with reasons |
| 11 | `docs/09-naming.md` | Thirty name candidates, selection criteria, the shortlist, and the collision-screen status |
| 12 | `docs/10-decisions-for-owner.md` | The 26 decisions that are the owner's, each with options, a recommendation, and why; mirrored as ADRs under `docs/adr/` |
| 13 | `docs/11-open-questions-and-unverified.md` | Verification method and labels, every correction adopted, every statement still unverified, and the completeness critique |
| 14 | `docs/12-implementation-and-verification.md` | How this repository was produced and how to check it |
| 15 | `docs/14-beta-test-report.md` | The beta test of the clickable prototype: the falsifiable claim, thresholds pre-registered before any session, the 30-persona panel, declared constraints, what the exercise cannot prove, and the results |
| 16 | `docs/15-function-audit.md` | The function-by-function audit of the prototype: the rules for operational, consistent, and clear registered before any agent ran, the 497-function inventory, the method, one row per function with its status and evidence, the 248 root causes fixed, and what the fix round found about the audit's own instruments |
| 17 | `docs/16-ux-review.md` | The UI and UX review against the research on intuitive design and cognitive load: the method and 117 heuristics registered before any finding, the audit of every screen with each root cause re-measured by an adversarial verifier, the three waves of fixes, and the before-and-after numbers |
| 18 | `docs/17-competitive-enhancement-review.md` | Owner review of 2026-09-14: competitive position, five public pillars (comprehensive PMS, beauty, cognitive load, one-way SuperByte with an expanded twin knowledge base, Precog as risk-avoidance), In / Later / Not, accepted ADRs 2 and 12, and Increments 0.1–0.4 |

## Status of the open decisions

Every decision in `docs/10` carries a recommendation. The recommendations stand unless the owner changes them. The one repository action that needed explicit permission (creating `main` so a pull request has a base) was granted by approving the plan.

## Verification labels

Legal and regulatory statements carry one of four labels wherever they appear:

- **PRIMARY**: read from the official text (none in this pass; the official domains were blocked from the planning environment).
- **SECONDARY**: confirmed through search snippets of reputable secondary sources, or by the workflow's verifier agents working from the same kind of sources.
- **REPO**: asserted in the dental repository's knowledge files and not independently re-verified.
- **UNVERIFIED**: asserted by a draft and not checked, or checked and found unverifiable from this environment.

Of 127 statements checked: 62 confirmed (SECONDARY), 17 corrected, 1 refuted and reversed, 47 unverifiable and listed for counsel in `docs/11`.

## Evidence

- `knowledge/` is the market-research knowledge base (v3, 2026-09-02) that the plan builds on: `INDEX.md`, `semantic-memory.md`, 31 source cards under `sources/`, and the full report.
- `knowledge/reviews/` holds the 44 planning-agent outputs rendered to Markdown (explorer reports, design lenses, judges, design synthesis, security drafts, citation verdicts, security synthesis, naming sets, collision screen, critique), the approved plan itself, and the 13 feature-workflow outputs behind `docs/13` (`features-*`). Every conclusion in `docs/` traces to one of these files.

## Prototype and beta

`prototype/` is a static, dependency-free clickable prototype of the merged product built from `docs/04` and `docs/13`: the seven persona homes, the five daily flows, and the six signature moments, on synthetic data for one tenant with three locations. Open `prototype/index.html` directly or run `npx serve prototype -l 4173`. `prototype/CONTRACTS.md` names every route, `data-testid`, refusal code, and seed id, and defines the in-page event log (`window.__events`) that every check and every panel finding cites.

The prototype needs nothing installed. The two browser-driven checks below need Playwright, the repository's only dependency: `npm run setup` installs it and its Chromium build (or `npm install && npx playwright install chromium`). Reports land under a gitignored `.scratch/` at the repository root unless `SCRATCH` or `--out`/`--json` says otherwise. `npm test` runs the syntax gate and then all four harnesses in turn.

- `node scripts/syntax-check.mjs` parses every `prototype/js` and `scripts` file with `node --check`. It exists because two merges concatenated both sides of the same JavaScript files and shipped a tree that could not boot; Playwright then timed out instead of naming the SyntaxError. It exits 0 only when every file parses.
- `node scripts/a11y-check.mjs` (`npm run a11y`) runs axe-core 4.13.0, vendored under `scripts/vendor/axe-core/` (MPL-2.0), over 24 contexts per theme and width: every persona home, the deep screens, the surfaces that open over them, and the states a press opens (an expanded tile, an open write-off, a standing filing gate, the Settings dialog), with the WCAG 2.0, 2.1 and 2.2 A and AA rule sets plus axe's best-practice rules. It exits 1 on any serious or critical violation and lists what axe could not decide, which is never read as a pass. `docs/16-ux-review.md` holds the review it was built for.
- `node scripts/proto-check.mjs` runs the UI stability checks (console errors, the five flows keyboard-only within their click budgets, 44 px targets with 8 px gaps, WCAG contrast in both themes, horizontal overflow at three widths, reduced motion, visible focus and no keyboard traps, test-id coverage, screenshots). It exits 0 only when every row passes.
- `node scripts/gen-roster.mjs --seed 20260903` regenerates the 30-persona panel byte-identically into `knowledge/reviews/beta-panel-roster.json` and one card per persona under `knowledge/reviews/beta-panel/cards/`.
- `scripts/beta/tasks/*.json` are the pre-registered task scripts per profession; `scripts/beta/validate-session.mjs` rejects a session file whose findings lack event-log `seq` ranges.
- `knowledge/reviews/beta-sessions/` holds one file per persona session, the verifier files, the dedup and severity table, the synthesis, and the critic's file. `docs/14-beta-test-report.md` states the verdict against the thresholds registered before the panel ran.
- `node scripts/beta/reproduce.mjs` runs the adversarial reproduction checks: the 43 from the beta panel's root causes, plus one module per prototype file under `scripts/beta/audit/` from the function audit, plus `misc-2` for the defects a later static read found, plus `storm-*` through `storm4-*` (one module per fixer in the four fix rounds of the beta storms; see docs/15 "Five beta storms"), plus the swarm modules and the `P-*` stability/performance gates. `--only <id,id>` runs a subset and `--json <path>` writes the evidence. Default position is "not reproduced"; a check must measure the breach. A check that throws, or that finishes without recording a result, is reported as `CRASH` or `NOTHING` rather than as a pass, and makes the run exit non-zero: a check that cannot fail is worse than no check. `docs/15-function-audit.md` holds the audit's rules, inventory, and one row per function.
- `node scripts/audit/inventory.mjs` lists every function in `prototype/js` with its file and line; `node scripts/audit/fixes.mjs` derives each root cause's fix state from the harness rather than from a claim; `node scripts/audit/report.mjs` builds the Results section of `docs/15` from the audit's own result files.

## Checking this repository

```
bash scripts/verify-docs.sh
```

To run every gate at once before a tree goes live, `npm run review` (`scripts/final-review.sh`) runs the syntax gate, proto-check, the accessibility check, verify-docs and the full regression suite in that order, writes each report under `.scratch/final-review/`, prints one line per gate and exits 1 if any is red.

The script checks that every relative link resolves, that `knowledge/INDEX.md` lists every source and review file exactly once, that the regulatory tables in `docs/06` carry a verification label on every row, that every repository path named in `docs/00` exists in a checkout of `dental` or `precog` (pass the checkout paths as arguments), that `docs/08` has six phases each with scope, exit criteria, and dependencies, and that the decision table in `docs/10` matches the ADR set.

## Terminology

The documents use one term per concept: **the PMS** (the merged product), **tenant** (one practice), **location** (one office of a tenant), **encounter** (one visit), **ledger entry** (one append-only money row), **dual release** (a second approver required before a money-moving entry posts), **owner**, **office manager**, **biller**, **hygienist**, **front-desk coordinator**.
